
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hed8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hedc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf00
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf04
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf08
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf0c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf10
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf14
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf18
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf1c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf20
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf24
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf28
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf2c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf30
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf34
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf38
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf3c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf40
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf44
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf48
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf4c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf50
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf54
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf58
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf5c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf80
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf84
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf88
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf8c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf90
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf94
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf98
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hf9c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hfa0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hfa4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hfa8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hfac
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hfb0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hfb4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hfb8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hfbc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hfc0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hfc4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hfc8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hfcc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hfd0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hfd4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hfd8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &Hfdc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class




'***********************************

' Regset Instance

'***********************************

Class EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_INSTANCE

    Public xform_rec0_w0
    Public xform_rec0_w1
    Public xform_rec0_w2
    Public xform_rec0_w3
    Public xform_rec0_w4
    Public xform_rec0_w5
    Public xform_rec0_w6
    Public xform_rec0_w7
    Public xform_rec0_w8
    Public xform_rec0_w9
    Public xform_rec0_w10
    Public xform_rec0_w11
    Public xform_rec0_w12
    Public xform_rec0_w13
    Public xform_rec0_w14
    Public xform_rec0_w15
    Public xform_rec0_w16
    Public xform_rec0_w17
    Public xform_rec0_w18
    Public xform_rec0_w19
    Public xform_rec0_w20
    Public xform_rec0_w21
    Public xform_rec0_w22
    Public xform_rec0_w23
    Public xform_rec1_w0
    Public xform_rec1_w1
    Public xform_rec1_w2
    Public xform_rec1_w3
    Public xform_rec1_w4
    Public xform_rec1_w5
    Public xform_rec1_w6
    Public xform_rec1_w7
    Public xform_rec1_w8
    Public xform_rec1_w9
    Public xform_rec1_w10
    Public xform_rec1_w11
    Public xform_rec1_w12
    Public xform_rec1_w13
    Public xform_rec1_w14
    Public xform_rec1_w15
    Public xform_rec1_w16
    Public xform_rec1_w17
    Public xform_rec1_w18
    Public xform_rec1_w19
    Public xform_rec1_w20
    Public xform_rec1_w21
    Public xform_rec1_w22
    Public xform_rec1_w23
    Public xform_rec2_w0
    Public xform_rec2_w1
    Public xform_rec2_w2
    Public xform_rec2_w3
    Public xform_rec2_w4
    Public xform_rec2_w5
    Public xform_rec2_w6
    Public xform_rec2_w7
    Public xform_rec2_w8
    Public xform_rec2_w9
    Public xform_rec2_w10
    Public xform_rec2_w11
    Public xform_rec2_w12
    Public xform_rec2_w13
    Public xform_rec2_w14
    Public xform_rec2_w15
    Public xform_rec2_w16
    Public xform_rec2_w17
    Public xform_rec2_w18
    Public xform_rec2_w19
    Public xform_rec2_w20
    Public xform_rec2_w21
    Public xform_rec2_w22
    Public xform_rec2_w23
    Public xform_rec3_w0
    Public xform_rec3_w1
    Public xform_rec3_w2
    Public xform_rec3_w3
    Public xform_rec3_w4
    Public xform_rec3_w5
    Public xform_rec3_w6
    Public xform_rec3_w7
    Public xform_rec3_w8
    Public xform_rec3_w9
    Public xform_rec3_w10
    Public xform_rec3_w11
    Public xform_rec3_w12
    Public xform_rec3_w13
    Public xform_rec3_w14
    Public xform_rec3_w15
    Public xform_rec3_w16
    Public xform_rec3_w17
    Public xform_rec3_w18
    Public xform_rec3_w19
    Public xform_rec3_w20
    Public xform_rec3_w21
    Public xform_rec3_w22
    Public xform_rec3_w23
    Public xform_rec4_w0
    Public xform_rec4_w1
    Public xform_rec4_w2
    Public xform_rec4_w3
    Public xform_rec4_w4
    Public xform_rec4_w5
    Public xform_rec4_w6
    Public xform_rec4_w7
    Public xform_rec4_w8
    Public xform_rec4_w9
    Public xform_rec4_w10
    Public xform_rec4_w11
    Public xform_rec4_w12
    Public xform_rec4_w13
    Public xform_rec4_w14
    Public xform_rec4_w15
    Public xform_rec4_w16
    Public xform_rec28_w21
    Public xform_rec28_w22
    Public xform_rec28_w23
    Public xform_rec29_w0
    Public xform_rec29_w1
    Public xform_rec29_w2
    Public xform_rec29_w3
    Public xform_rec29_w4
    Public xform_rec29_w5
    Public xform_rec29_w6
    Public xform_rec29_w7
    Public xform_rec29_w8
    Public xform_rec29_w9
    Public xform_rec29_w10
    Public xform_rec29_w11
    Public xform_rec29_w12
    Public xform_rec29_w13
    Public xform_rec29_w14
    Public xform_rec29_w15
    Public xform_rec29_w16
    Public xform_rec29_w17
    Public xform_rec29_w18
    Public xform_rec29_w19
    Public xform_rec29_w20
    Public xform_rec29_w21
    Public xform_rec29_w22
    Public xform_rec29_w23
    Public xform_rec30_w0
    Public xform_rec30_w1
    Public xform_rec30_w2
    Public xform_rec30_w3
    Public xform_rec30_w4
    Public xform_rec30_w5
    Public xform_rec30_w6
    Public xform_rec30_w7
    Public xform_rec30_w8
    Public xform_rec30_w9
    Public xform_rec30_w10
    Public xform_rec30_w11
    Public xform_rec30_w12
    Public xform_rec30_w13
    Public xform_rec30_w14
    Public xform_rec30_w15
    Public xform_rec30_w16
    Public xform_rec30_w17
    Public xform_rec30_w18
    Public xform_rec30_w19
    Public xform_rec30_w20
    Public xform_rec30_w21
    Public xform_rec30_w22
    Public xform_rec30_w23
    Public xform_rec31_w0
    Public xform_rec31_w1
    Public xform_rec31_w2
    Public xform_rec31_w3
    Public xform_rec31_w4
    Public xform_rec31_w5
    Public xform_rec31_w6
    Public xform_rec31_w7
    Public xform_rec31_w8
    Public xform_rec31_w9
    Public xform_rec31_w10
    Public xform_rec31_w11
    Public xform_rec31_w12
    Public xform_rec31_w13
    Public xform_rec31_w14
    Public xform_rec31_w15
    Public xform_rec31_w16
    Public xform_rec31_w17
    Public xform_rec31_w18
    Public xform_rec31_w19
    Public xform_rec31_w20
    Public xform_rec31_w21
    Public xform_rec31_w22
    Public xform_rec31_w23


    Public default function Init(aBaseAddr)
        Set xform_rec0_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w0)(aBaseAddr, 32)
        Set xform_rec0_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w1)(aBaseAddr, 32)
        Set xform_rec0_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w2)(aBaseAddr, 32)
        Set xform_rec0_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w3)(aBaseAddr, 32)
        Set xform_rec0_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w4)(aBaseAddr, 32)
        Set xform_rec0_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w5)(aBaseAddr, 32)
        Set xform_rec0_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w6)(aBaseAddr, 32)
        Set xform_rec0_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w7)(aBaseAddr, 32)
        Set xform_rec0_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w8)(aBaseAddr, 32)
        Set xform_rec0_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w9)(aBaseAddr, 32)
        Set xform_rec0_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w10)(aBaseAddr, 32)
        Set xform_rec0_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w11)(aBaseAddr, 32)
        Set xform_rec0_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w12)(aBaseAddr, 32)
        Set xform_rec0_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w13)(aBaseAddr, 32)
        Set xform_rec0_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w14)(aBaseAddr, 32)
        Set xform_rec0_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w15)(aBaseAddr, 32)
        Set xform_rec0_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w16)(aBaseAddr, 32)
        Set xform_rec0_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w17)(aBaseAddr, 32)
        Set xform_rec0_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w18)(aBaseAddr, 32)
        Set xform_rec0_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w19)(aBaseAddr, 32)
        Set xform_rec0_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w20)(aBaseAddr, 32)
        Set xform_rec0_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w21)(aBaseAddr, 32)
        Set xform_rec0_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w22)(aBaseAddr, 32)
        Set xform_rec0_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w23)(aBaseAddr, 32)
        Set xform_rec1_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w0)(aBaseAddr, 32)
        Set xform_rec1_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w1)(aBaseAddr, 32)
        Set xform_rec1_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w2)(aBaseAddr, 32)
        Set xform_rec1_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w3)(aBaseAddr, 32)
        Set xform_rec1_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w4)(aBaseAddr, 32)
        Set xform_rec1_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w5)(aBaseAddr, 32)
        Set xform_rec1_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w6)(aBaseAddr, 32)
        Set xform_rec1_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w7)(aBaseAddr, 32)
        Set xform_rec1_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w8)(aBaseAddr, 32)
        Set xform_rec1_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w9)(aBaseAddr, 32)
        Set xform_rec1_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w10)(aBaseAddr, 32)
        Set xform_rec1_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w11)(aBaseAddr, 32)
        Set xform_rec1_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w12)(aBaseAddr, 32)
        Set xform_rec1_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w13)(aBaseAddr, 32)
        Set xform_rec1_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w14)(aBaseAddr, 32)
        Set xform_rec1_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w15)(aBaseAddr, 32)
        Set xform_rec1_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w16)(aBaseAddr, 32)
        Set xform_rec1_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w17)(aBaseAddr, 32)
        Set xform_rec1_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w18)(aBaseAddr, 32)
        Set xform_rec1_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w19)(aBaseAddr, 32)
        Set xform_rec1_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w20)(aBaseAddr, 32)
        Set xform_rec1_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w21)(aBaseAddr, 32)
        Set xform_rec1_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w22)(aBaseAddr, 32)
        Set xform_rec1_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w23)(aBaseAddr, 32)
        Set xform_rec2_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w0)(aBaseAddr, 32)
        Set xform_rec2_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w1)(aBaseAddr, 32)
        Set xform_rec2_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w2)(aBaseAddr, 32)
        Set xform_rec2_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w3)(aBaseAddr, 32)
        Set xform_rec2_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w4)(aBaseAddr, 32)
        Set xform_rec2_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w5)(aBaseAddr, 32)
        Set xform_rec2_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w6)(aBaseAddr, 32)
        Set xform_rec2_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w7)(aBaseAddr, 32)
        Set xform_rec2_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w8)(aBaseAddr, 32)
        Set xform_rec2_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w9)(aBaseAddr, 32)
        Set xform_rec2_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w10)(aBaseAddr, 32)
        Set xform_rec2_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w11)(aBaseAddr, 32)
        Set xform_rec2_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w12)(aBaseAddr, 32)
        Set xform_rec2_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w13)(aBaseAddr, 32)
        Set xform_rec2_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w14)(aBaseAddr, 32)
        Set xform_rec2_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w15)(aBaseAddr, 32)
        Set xform_rec2_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w16)(aBaseAddr, 32)
        Set xform_rec2_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w17)(aBaseAddr, 32)
        Set xform_rec2_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w18)(aBaseAddr, 32)
        Set xform_rec2_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w19)(aBaseAddr, 32)
        Set xform_rec2_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w20)(aBaseAddr, 32)
        Set xform_rec2_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w21)(aBaseAddr, 32)
        Set xform_rec2_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w22)(aBaseAddr, 32)
        Set xform_rec2_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w23)(aBaseAddr, 32)
        Set xform_rec3_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w0)(aBaseAddr, 32)
        Set xform_rec3_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w1)(aBaseAddr, 32)
        Set xform_rec3_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w2)(aBaseAddr, 32)
        Set xform_rec3_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w3)(aBaseAddr, 32)
        Set xform_rec3_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w4)(aBaseAddr, 32)
        Set xform_rec3_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w5)(aBaseAddr, 32)
        Set xform_rec3_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w6)(aBaseAddr, 32)
        Set xform_rec3_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w7)(aBaseAddr, 32)
        Set xform_rec3_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w8)(aBaseAddr, 32)
        Set xform_rec3_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w9)(aBaseAddr, 32)
        Set xform_rec3_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w10)(aBaseAddr, 32)
        Set xform_rec3_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w11)(aBaseAddr, 32)
        Set xform_rec3_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w12)(aBaseAddr, 32)
        Set xform_rec3_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w13)(aBaseAddr, 32)
        Set xform_rec3_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w14)(aBaseAddr, 32)
        Set xform_rec3_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w15)(aBaseAddr, 32)
        Set xform_rec3_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w16)(aBaseAddr, 32)
        Set xform_rec3_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w17)(aBaseAddr, 32)
        Set xform_rec3_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w18)(aBaseAddr, 32)
        Set xform_rec3_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w19)(aBaseAddr, 32)
        Set xform_rec3_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w20)(aBaseAddr, 32)
        Set xform_rec3_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w21)(aBaseAddr, 32)
        Set xform_rec3_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w22)(aBaseAddr, 32)
        Set xform_rec3_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w23)(aBaseAddr, 32)
        Set xform_rec4_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w0)(aBaseAddr, 32)
        Set xform_rec4_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w1)(aBaseAddr, 32)
        Set xform_rec4_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w2)(aBaseAddr, 32)
        Set xform_rec4_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w3)(aBaseAddr, 32)
        Set xform_rec4_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w4)(aBaseAddr, 32)
        Set xform_rec4_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w5)(aBaseAddr, 32)
        Set xform_rec4_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w6)(aBaseAddr, 32)
        Set xform_rec4_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w7)(aBaseAddr, 32)
        Set xform_rec4_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w8)(aBaseAddr, 32)
        Set xform_rec4_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w9)(aBaseAddr, 32)
        Set xform_rec4_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w10)(aBaseAddr, 32)
        Set xform_rec4_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w11)(aBaseAddr, 32)
        Set xform_rec4_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w12)(aBaseAddr, 32)
        Set xform_rec4_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w13)(aBaseAddr, 32)
        Set xform_rec4_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w14)(aBaseAddr, 32)
        Set xform_rec4_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w15)(aBaseAddr, 32)
        Set xform_rec4_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w16)(aBaseAddr, 32)
        Set xform_rec4_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w17)(aBaseAddr, 32)
        Set xform_rec4_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w18)(aBaseAddr, 32)
        Set xform_rec4_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w19)(aBaseAddr, 32)
        Set xform_rec4_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w20)(aBaseAddr, 32)
        Set xform_rec4_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w21)(aBaseAddr, 32)
        Set xform_rec4_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w22)(aBaseAddr, 32)
        Set xform_rec4_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w23)(aBaseAddr, 32)
        Set xform_rec5_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w0)(aBaseAddr, 32)
        Set xform_rec5_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w1)(aBaseAddr, 32)
        Set xform_rec5_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w2)(aBaseAddr, 32)
        Set xform_rec5_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w3)(aBaseAddr, 32)
        Set xform_rec5_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w4)(aBaseAddr, 32)
        Set xform_rec5_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w5)(aBaseAddr, 32)
        Set xform_rec5_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w6)(aBaseAddr, 32)
        Set xform_rec5_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w7)(aBaseAddr, 32)
        Set xform_rec5_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w8)(aBaseAddr, 32)
        Set xform_rec5_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w9)(aBaseAddr, 32)
        Set xform_rec5_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w10)(aBaseAddr, 32)
        Set xform_rec5_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w11)(aBaseAddr, 32)
        Set xform_rec5_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w12)(aBaseAddr, 32)
        Set xform_rec5_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w13)(aBaseAddr, 32)
        Set xform_rec5_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w14)(aBaseAddr, 32)
        Set xform_rec5_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w15)(aBaseAddr, 32)
        Set xform_rec5_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w16)(aBaseAddr, 32)
        Set xform_rec5_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w17)(aBaseAddr, 32)
        Set xform_rec5_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w18)(aBaseAddr, 32)
        Set xform_rec5_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w19)(aBaseAddr, 32)
        Set xform_rec5_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w20)(aBaseAddr, 32)
        Set xform_rec5_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w21)(aBaseAddr, 32)
        Set xform_rec5_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w22)(aBaseAddr, 32)
        Set xform_rec5_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w23)(aBaseAddr, 32)
        Set xform_rec6_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w0)(aBaseAddr, 32)
        Set xform_rec6_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w1)(aBaseAddr, 32)
        Set xform_rec6_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w2)(aBaseAddr, 32)
        Set xform_rec6_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w3)(aBaseAddr, 32)
        Set xform_rec6_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w4)(aBaseAddr, 32)
        Set xform_rec6_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w5)(aBaseAddr, 32)
        Set xform_rec6_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w6)(aBaseAddr, 32)
        Set xform_rec6_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w7)(aBaseAddr, 32)
        Set xform_rec6_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w8)(aBaseAddr, 32)
        Set xform_rec6_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w9)(aBaseAddr, 32)
        Set xform_rec6_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w10)(aBaseAddr, 32)
        Set xform_rec6_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w11)(aBaseAddr, 32)
        Set xform_rec6_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w12)(aBaseAddr, 32)
        Set xform_rec6_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w13)(aBaseAddr, 32)
        Set xform_rec6_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w14)(aBaseAddr, 32)
        Set xform_rec6_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w15)(aBaseAddr, 32)
        Set xform_rec6_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w16)(aBaseAddr, 32)
        Set xform_rec6_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w17)(aBaseAddr, 32)
        Set xform_rec6_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w18)(aBaseAddr, 32)
        Set xform_rec6_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w19)(aBaseAddr, 32)
        Set xform_rec6_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w20)(aBaseAddr, 32)
        Set xform_rec6_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w21)(aBaseAddr, 32)
        Set xform_rec6_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w22)(aBaseAddr, 32)
        Set xform_rec6_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w23)(aBaseAddr, 32)
        Set xform_rec7_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w0)(aBaseAddr, 32)
        Set xform_rec7_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w1)(aBaseAddr, 32)
        Set xform_rec7_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w2)(aBaseAddr, 32)
        Set xform_rec7_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w3)(aBaseAddr, 32)
        Set xform_rec7_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w4)(aBaseAddr, 32)
        Set xform_rec7_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w5)(aBaseAddr, 32)
        Set xform_rec7_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w6)(aBaseAddr, 32)
        Set xform_rec7_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w7)(aBaseAddr, 32)
        Set xform_rec7_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w8)(aBaseAddr, 32)
        Set xform_rec7_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w9)(aBaseAddr, 32)
        Set xform_rec7_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w10)(aBaseAddr, 32)
        Set xform_rec7_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w11)(aBaseAddr, 32)
        Set xform_rec7_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w12)(aBaseAddr, 32)
        Set xform_rec7_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w13)(aBaseAddr, 32)
        Set xform_rec7_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w14)(aBaseAddr, 32)
        Set xform_rec7_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w15)(aBaseAddr, 32)
        Set xform_rec7_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w16)(aBaseAddr, 32)
        Set xform_rec7_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w17)(aBaseAddr, 32)
        Set xform_rec7_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w18)(aBaseAddr, 32)
        Set xform_rec7_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w19)(aBaseAddr, 32)
        Set xform_rec7_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w20)(aBaseAddr, 32)
        Set xform_rec7_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w21)(aBaseAddr, 32)
        Set xform_rec7_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w22)(aBaseAddr, 32)
        Set xform_rec7_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w23)(aBaseAddr, 32)
        Set xform_rec8_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w0)(aBaseAddr, 32)
        Set xform_rec8_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w1)(aBaseAddr, 32)
        Set xform_rec8_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w2)(aBaseAddr, 32)
        Set xform_rec8_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w3)(aBaseAddr, 32)
        Set xform_rec8_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w4)(aBaseAddr, 32)
        Set xform_rec8_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w5)(aBaseAddr, 32)
        Set xform_rec8_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w6)(aBaseAddr, 32)
        Set xform_rec8_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w7)(aBaseAddr, 32)
        Set xform_rec8_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w8)(aBaseAddr, 32)
        Set xform_rec8_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w9)(aBaseAddr, 32)
        Set xform_rec8_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w10)(aBaseAddr, 32)
        Set xform_rec8_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w11)(aBaseAddr, 32)
        Set xform_rec8_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w12)(aBaseAddr, 32)
        Set xform_rec8_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w13)(aBaseAddr, 32)
        Set xform_rec8_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w14)(aBaseAddr, 32)
        Set xform_rec8_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w15)(aBaseAddr, 32)
        Set xform_rec8_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w16)(aBaseAddr, 32)
        Set xform_rec8_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w17)(aBaseAddr, 32)
        Set xform_rec8_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w18)(aBaseAddr, 32)
        Set xform_rec8_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w19)(aBaseAddr, 32)
        Set xform_rec8_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w20)(aBaseAddr, 32)
        Set xform_rec8_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w21)(aBaseAddr, 32)
        Set xform_rec8_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w22)(aBaseAddr, 32)
        Set xform_rec8_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w23)(aBaseAddr, 32)
        Set xform_rec9_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w0)(aBaseAddr, 32)
        Set xform_rec9_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w1)(aBaseAddr, 32)
        Set xform_rec9_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w2)(aBaseAddr, 32)
        Set xform_rec9_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w3)(aBaseAddr, 32)
        Set xform_rec9_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w4)(aBaseAddr, 32)
        Set xform_rec9_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w5)(aBaseAddr, 32)
        Set xform_rec9_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w6)(aBaseAddr, 32)
        Set xform_rec9_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w7)(aBaseAddr, 32)
        Set xform_rec9_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w8)(aBaseAddr, 32)
        Set xform_rec9_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w9)(aBaseAddr, 32)
        Set xform_rec9_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w10)(aBaseAddr, 32)
        Set xform_rec9_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w11)(aBaseAddr, 32)
        Set xform_rec9_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w12)(aBaseAddr, 32)
        Set xform_rec9_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w13)(aBaseAddr, 32)
        Set xform_rec9_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w14)(aBaseAddr, 32)
        Set xform_rec9_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w15)(aBaseAddr, 32)
        Set xform_rec9_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w16)(aBaseAddr, 32)
        Set xform_rec9_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w17)(aBaseAddr, 32)
        Set xform_rec9_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w18)(aBaseAddr, 32)
        Set xform_rec9_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w19)(aBaseAddr, 32)
        Set xform_rec9_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w20)(aBaseAddr, 32)
        Set xform_rec9_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w21)(aBaseAddr, 32)
        Set xform_rec9_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w22)(aBaseAddr, 32)
        Set xform_rec9_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w23)(aBaseAddr, 32)
        Set xform_rec10_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w0)(aBaseAddr, 32)
        Set xform_rec10_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w1)(aBaseAddr, 32)
        Set xform_rec10_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w2)(aBaseAddr, 32)
        Set xform_rec10_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w3)(aBaseAddr, 32)
        Set xform_rec10_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w4)(aBaseAddr, 32)
        Set xform_rec10_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w5)(aBaseAddr, 32)
        Set xform_rec10_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w6)(aBaseAddr, 32)
        Set xform_rec10_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w7)(aBaseAddr, 32)
        Set xform_rec10_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w8)(aBaseAddr, 32)
        Set xform_rec10_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w9)(aBaseAddr, 32)
        Set xform_rec10_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w10)(aBaseAddr, 32)
        Set xform_rec10_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w11)(aBaseAddr, 32)
        Set xform_rec10_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w12)(aBaseAddr, 32)
        Set xform_rec10_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w13)(aBaseAddr, 32)
        Set xform_rec10_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w14)(aBaseAddr, 32)
        Set xform_rec10_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w15)(aBaseAddr, 32)
        Set xform_rec10_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w16)(aBaseAddr, 32)
        Set xform_rec10_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w17)(aBaseAddr, 32)
        Set xform_rec10_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w18)(aBaseAddr, 32)
        Set xform_rec10_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w19)(aBaseAddr, 32)
        Set xform_rec10_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w20)(aBaseAddr, 32)
        Set xform_rec10_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w21)(aBaseAddr, 32)
        Set xform_rec10_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w22)(aBaseAddr, 32)
        Set xform_rec10_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w23)(aBaseAddr, 32)
        Set xform_rec11_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w0)(aBaseAddr, 32)
        Set xform_rec11_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w1)(aBaseAddr, 32)
        Set xform_rec11_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w2)(aBaseAddr, 32)
        Set xform_rec11_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w3)(aBaseAddr, 32)
        Set xform_rec11_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w4)(aBaseAddr, 32)
        Set xform_rec11_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w5)(aBaseAddr, 32)
        Set xform_rec11_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w6)(aBaseAddr, 32)
        Set xform_rec11_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w7)(aBaseAddr, 32)
        Set xform_rec11_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w8)(aBaseAddr, 32)
        Set xform_rec11_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w9)(aBaseAddr, 32)
        Set xform_rec11_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w10)(aBaseAddr, 32)
        Set xform_rec11_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w11)(aBaseAddr, 32)
        Set xform_rec11_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w12)(aBaseAddr, 32)
        Set xform_rec11_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w13)(aBaseAddr, 32)
        Set xform_rec11_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w14)(aBaseAddr, 32)
        Set xform_rec11_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w15)(aBaseAddr, 32)
        Set xform_rec11_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w16)(aBaseAddr, 32)
        Set xform_rec11_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w17)(aBaseAddr, 32)
        Set xform_rec11_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w18)(aBaseAddr, 32)
        Set xform_rec11_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w19)(aBaseAddr, 32)
        Set xform_rec11_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w20)(aBaseAddr, 32)
        Set xform_rec11_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w21)(aBaseAddr, 32)
        Set xform_rec11_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w22)(aBaseAddr, 32)
        Set xform_rec11_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w23)(aBaseAddr, 32)
        Set xform_rec12_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w0)(aBaseAddr, 32)
        Set xform_rec12_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w1)(aBaseAddr, 32)
        Set xform_rec12_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w2)(aBaseAddr, 32)
        Set xform_rec12_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w3)(aBaseAddr, 32)
        Set xform_rec12_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w4)(aBaseAddr, 32)
        Set xform_rec12_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w5)(aBaseAddr, 32)
        Set xform_rec12_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w6)(aBaseAddr, 32)
        Set xform_rec12_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w7)(aBaseAddr, 32)
        Set xform_rec12_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w8)(aBaseAddr, 32)
        Set xform_rec12_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w9)(aBaseAddr, 32)
        Set xform_rec12_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w10)(aBaseAddr, 32)
        Set xform_rec12_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w11)(aBaseAddr, 32)
        Set xform_rec12_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w12)(aBaseAddr, 32)
        Set xform_rec12_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w13)(aBaseAddr, 32)
        Set xform_rec12_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w14)(aBaseAddr, 32)
        Set xform_rec12_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w15)(aBaseAddr, 32)
        Set xform_rec12_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w16)(aBaseAddr, 32)
        Set xform_rec12_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w17)(aBaseAddr, 32)
        Set xform_rec12_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w18)(aBaseAddr, 32)
        Set xform_rec12_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w19)(aBaseAddr, 32)
        Set xform_rec12_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w20)(aBaseAddr, 32)
        Set xform_rec12_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w21)(aBaseAddr, 32)
        Set xform_rec12_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w22)(aBaseAddr, 32)
        Set xform_rec12_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w23)(aBaseAddr, 32)
        Set xform_rec13_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w0)(aBaseAddr, 32)
        Set xform_rec13_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w1)(aBaseAddr, 32)
        Set xform_rec13_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w2)(aBaseAddr, 32)
        Set xform_rec13_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w3)(aBaseAddr, 32)
        Set xform_rec13_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w4)(aBaseAddr, 32)
        Set xform_rec13_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w5)(aBaseAddr, 32)
        Set xform_rec13_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w6)(aBaseAddr, 32)
        Set xform_rec13_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w7)(aBaseAddr, 32)
        Set xform_rec13_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w8)(aBaseAddr, 32)
        Set xform_rec13_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w9)(aBaseAddr, 32)
        Set xform_rec13_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w10)(aBaseAddr, 32)
        Set xform_rec13_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w11)(aBaseAddr, 32)
        Set xform_rec13_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w12)(aBaseAddr, 32)
        Set xform_rec13_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w13)(aBaseAddr, 32)
        Set xform_rec13_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w14)(aBaseAddr, 32)
        Set xform_rec13_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w15)(aBaseAddr, 32)
        Set xform_rec13_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w16)(aBaseAddr, 32)
        Set xform_rec13_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w17)(aBaseAddr, 32)
        Set xform_rec13_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w18)(aBaseAddr, 32)
        Set xform_rec13_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w19)(aBaseAddr, 32)
        Set xform_rec13_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w20)(aBaseAddr, 32)
        Set xform_rec13_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w21)(aBaseAddr, 32)
        Set xform_rec13_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w22)(aBaseAddr, 32)
        Set xform_rec13_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w23)(aBaseAddr, 32)
        Set xform_rec14_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w0)(aBaseAddr, 32)
        Set xform_rec14_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w1)(aBaseAddr, 32)
        Set xform_rec14_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w2)(aBaseAddr, 32)
        Set xform_rec14_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w3)(aBaseAddr, 32)
        Set xform_rec14_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w4)(aBaseAddr, 32)
        Set xform_rec14_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w5)(aBaseAddr, 32)
        Set xform_rec14_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w6)(aBaseAddr, 32)
        Set xform_rec14_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w7)(aBaseAddr, 32)
        Set xform_rec14_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w8)(aBaseAddr, 32)
        Set xform_rec14_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w9)(aBaseAddr, 32)
        Set xform_rec14_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w10)(aBaseAddr, 32)
        Set xform_rec14_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w11)(aBaseAddr, 32)
        Set xform_rec14_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w12)(aBaseAddr, 32)
        Set xform_rec14_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w13)(aBaseAddr, 32)
        Set xform_rec14_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w14)(aBaseAddr, 32)
        Set xform_rec14_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w15)(aBaseAddr, 32)
        Set xform_rec14_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w16)(aBaseAddr, 32)
        Set xform_rec14_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w17)(aBaseAddr, 32)
        Set xform_rec14_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w18)(aBaseAddr, 32)
        Set xform_rec14_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w19)(aBaseAddr, 32)
        Set xform_rec14_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w20)(aBaseAddr, 32)
        Set xform_rec14_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w21)(aBaseAddr, 32)
        Set xform_rec14_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w22)(aBaseAddr, 32)
        Set xform_rec14_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w23)(aBaseAddr, 32)
        Set xform_rec15_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w0)(aBaseAddr, 32)
        Set xform_rec15_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w1)(aBaseAddr, 32)
        Set xform_rec15_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w2)(aBaseAddr, 32)
        Set xform_rec15_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w3)(aBaseAddr, 32)
        Set xform_rec15_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w4)(aBaseAddr, 32)
        Set xform_rec15_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w5)(aBaseAddr, 32)
        Set xform_rec15_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w6)(aBaseAddr, 32)
        Set xform_rec15_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w7)(aBaseAddr, 32)
        Set xform_rec15_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w8)(aBaseAddr, 32)
        Set xform_rec15_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w9)(aBaseAddr, 32)
        Set xform_rec15_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w10)(aBaseAddr, 32)
        Set xform_rec15_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w11)(aBaseAddr, 32)
        Set xform_rec15_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w12)(aBaseAddr, 32)
        Set xform_rec15_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w13)(aBaseAddr, 32)
        Set xform_rec15_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w14)(aBaseAddr, 32)
        Set xform_rec15_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w15)(aBaseAddr, 32)
        Set xform_rec15_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w16)(aBaseAddr, 32)
        Set xform_rec15_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w17)(aBaseAddr, 32)
        Set xform_rec15_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w18)(aBaseAddr, 32)
        Set xform_rec15_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w19)(aBaseAddr, 32)
        Set xform_rec15_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w20)(aBaseAddr, 32)
        Set xform_rec15_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w21)(aBaseAddr, 32)
        Set xform_rec15_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w22)(aBaseAddr, 32)
        Set xform_rec15_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w23)(aBaseAddr, 32)
        Set xform_rec16_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w0)(aBaseAddr, 32)
        Set xform_rec16_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w1)(aBaseAddr, 32)
        Set xform_rec16_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w2)(aBaseAddr, 32)
        Set xform_rec16_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w3)(aBaseAddr, 32)
        Set xform_rec16_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w4)(aBaseAddr, 32)
        Set xform_rec16_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w5)(aBaseAddr, 32)
        Set xform_rec16_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w6)(aBaseAddr, 32)
        Set xform_rec16_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w7)(aBaseAddr, 32)
        Set xform_rec16_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w8)(aBaseAddr, 32)
        Set xform_rec16_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w9)(aBaseAddr, 32)
        Set xform_rec16_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w10)(aBaseAddr, 32)
        Set xform_rec16_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w11)(aBaseAddr, 32)
        Set xform_rec16_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w12)(aBaseAddr, 32)
        Set xform_rec16_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w13)(aBaseAddr, 32)
        Set xform_rec16_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w14)(aBaseAddr, 32)
        Set xform_rec16_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w15)(aBaseAddr, 32)
        Set xform_rec16_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w16)(aBaseAddr, 32)
        Set xform_rec16_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w17)(aBaseAddr, 32)
        Set xform_rec16_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w18)(aBaseAddr, 32)
        Set xform_rec16_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w19)(aBaseAddr, 32)
        Set xform_rec16_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w20)(aBaseAddr, 32)
        Set xform_rec16_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w21)(aBaseAddr, 32)
        Set xform_rec16_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w22)(aBaseAddr, 32)
        Set xform_rec16_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w23)(aBaseAddr, 32)
        Set xform_rec17_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w0)(aBaseAddr, 32)
        Set xform_rec17_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w1)(aBaseAddr, 32)
        Set xform_rec17_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w2)(aBaseAddr, 32)
        Set xform_rec17_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w3)(aBaseAddr, 32)
        Set xform_rec17_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w4)(aBaseAddr, 32)
        Set xform_rec17_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w5)(aBaseAddr, 32)
        Set xform_rec17_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w6)(aBaseAddr, 32)
        Set xform_rec17_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w7)(aBaseAddr, 32)
        Set xform_rec17_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w8)(aBaseAddr, 32)
        Set xform_rec17_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w9)(aBaseAddr, 32)
        Set xform_rec17_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w10)(aBaseAddr, 32)
        Set xform_rec17_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w11)(aBaseAddr, 32)
        Set xform_rec17_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w12)(aBaseAddr, 32)
        Set xform_rec17_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w13)(aBaseAddr, 32)
        Set xform_rec17_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w14)(aBaseAddr, 32)
        Set xform_rec17_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w15)(aBaseAddr, 32)
        Set xform_rec17_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w16)(aBaseAddr, 32)
        Set xform_rec17_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w17)(aBaseAddr, 32)
        Set xform_rec17_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w18)(aBaseAddr, 32)
        Set xform_rec17_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w19)(aBaseAddr, 32)
        Set xform_rec17_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w20)(aBaseAddr, 32)
        Set xform_rec17_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w21)(aBaseAddr, 32)
        Set xform_rec17_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w22)(aBaseAddr, 32)
        Set xform_rec17_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w23)(aBaseAddr, 32)
        Set xform_rec18_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w0)(aBaseAddr, 32)
        Set xform_rec18_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w1)(aBaseAddr, 32)
        Set xform_rec18_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w2)(aBaseAddr, 32)
        Set xform_rec18_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w3)(aBaseAddr, 32)
        Set xform_rec18_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w4)(aBaseAddr, 32)
        Set xform_rec18_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w5)(aBaseAddr, 32)
        Set xform_rec18_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w6)(aBaseAddr, 32)
        Set xform_rec18_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w7)(aBaseAddr, 32)
        Set xform_rec18_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w8)(aBaseAddr, 32)
        Set xform_rec18_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w9)(aBaseAddr, 32)
        Set xform_rec18_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w10)(aBaseAddr, 32)
        Set xform_rec18_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w11)(aBaseAddr, 32)
        Set xform_rec18_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w12)(aBaseAddr, 32)
        Set xform_rec18_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w13)(aBaseAddr, 32)
        Set xform_rec18_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w14)(aBaseAddr, 32)
        Set xform_rec18_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w15)(aBaseAddr, 32)
        Set xform_rec18_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w16)(aBaseAddr, 32)
        Set xform_rec18_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w17)(aBaseAddr, 32)
        Set xform_rec18_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w18)(aBaseAddr, 32)
        Set xform_rec18_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w19)(aBaseAddr, 32)
        Set xform_rec18_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w20)(aBaseAddr, 32)
        Set xform_rec18_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w21)(aBaseAddr, 32)
        Set xform_rec18_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w22)(aBaseAddr, 32)
        Set xform_rec18_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w23)(aBaseAddr, 32)
        Set xform_rec19_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w0)(aBaseAddr, 32)
        Set xform_rec19_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w1)(aBaseAddr, 32)
        Set xform_rec19_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w2)(aBaseAddr, 32)
        Set xform_rec19_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w3)(aBaseAddr, 32)
        Set xform_rec19_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w4)(aBaseAddr, 32)
        Set xform_rec19_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w5)(aBaseAddr, 32)
        Set xform_rec19_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w6)(aBaseAddr, 32)
        Set xform_rec19_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w7)(aBaseAddr, 32)
        Set xform_rec19_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w8)(aBaseAddr, 32)
        Set xform_rec19_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w9)(aBaseAddr, 32)
        Set xform_rec19_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w10)(aBaseAddr, 32)
        Set xform_rec19_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w11)(aBaseAddr, 32)
        Set xform_rec19_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w12)(aBaseAddr, 32)
        Set xform_rec19_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w13)(aBaseAddr, 32)
        Set xform_rec19_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w14)(aBaseAddr, 32)
        Set xform_rec19_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w15)(aBaseAddr, 32)
        Set xform_rec19_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w16)(aBaseAddr, 32)
        Set xform_rec19_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w17)(aBaseAddr, 32)
        Set xform_rec19_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w18)(aBaseAddr, 32)
        Set xform_rec19_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w19)(aBaseAddr, 32)
        Set xform_rec19_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w20)(aBaseAddr, 32)
        Set xform_rec19_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w21)(aBaseAddr, 32)
        Set xform_rec19_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w22)(aBaseAddr, 32)
        Set xform_rec19_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w23)(aBaseAddr, 32)
        Set xform_rec20_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w0)(aBaseAddr, 32)
        Set xform_rec20_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w1)(aBaseAddr, 32)
        Set xform_rec20_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w2)(aBaseAddr, 32)
        Set xform_rec20_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w3)(aBaseAddr, 32)
        Set xform_rec20_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w4)(aBaseAddr, 32)
        Set xform_rec20_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w5)(aBaseAddr, 32)
        Set xform_rec20_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w6)(aBaseAddr, 32)
        Set xform_rec20_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w7)(aBaseAddr, 32)
        Set xform_rec20_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w8)(aBaseAddr, 32)
        Set xform_rec20_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w9)(aBaseAddr, 32)
        Set xform_rec20_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w10)(aBaseAddr, 32)
        Set xform_rec20_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w11)(aBaseAddr, 32)
        Set xform_rec20_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w12)(aBaseAddr, 32)
        Set xform_rec20_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w13)(aBaseAddr, 32)
        Set xform_rec20_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w14)(aBaseAddr, 32)
        Set xform_rec20_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w15)(aBaseAddr, 32)
        Set xform_rec20_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w16)(aBaseAddr, 32)
        Set xform_rec20_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w17)(aBaseAddr, 32)
        Set xform_rec20_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w18)(aBaseAddr, 32)
        Set xform_rec20_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w19)(aBaseAddr, 32)
        Set xform_rec20_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w20)(aBaseAddr, 32)
        Set xform_rec20_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w21)(aBaseAddr, 32)
        Set xform_rec20_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w22)(aBaseAddr, 32)
        Set xform_rec20_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w23)(aBaseAddr, 32)
        Set xform_rec21_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w0)(aBaseAddr, 32)
        Set xform_rec21_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w1)(aBaseAddr, 32)
        Set xform_rec21_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w2)(aBaseAddr, 32)
        Set xform_rec21_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w3)(aBaseAddr, 32)
        Set xform_rec21_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w4)(aBaseAddr, 32)
        Set xform_rec21_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w5)(aBaseAddr, 32)
        Set xform_rec21_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w6)(aBaseAddr, 32)
        Set xform_rec21_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w7)(aBaseAddr, 32)
        Set xform_rec21_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w8)(aBaseAddr, 32)
        Set xform_rec21_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w9)(aBaseAddr, 32)
        Set xform_rec21_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w10)(aBaseAddr, 32)
        Set xform_rec21_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w11)(aBaseAddr, 32)
        Set xform_rec21_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w12)(aBaseAddr, 32)
        Set xform_rec21_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w13)(aBaseAddr, 32)
        Set xform_rec21_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w14)(aBaseAddr, 32)
        Set xform_rec21_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w15)(aBaseAddr, 32)
        Set xform_rec21_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w16)(aBaseAddr, 32)
        Set xform_rec21_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w17)(aBaseAddr, 32)
        Set xform_rec21_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w18)(aBaseAddr, 32)
        Set xform_rec21_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w19)(aBaseAddr, 32)
        Set xform_rec21_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w20)(aBaseAddr, 32)
        Set xform_rec21_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w21)(aBaseAddr, 32)
        Set xform_rec21_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w22)(aBaseAddr, 32)
        Set xform_rec21_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w23)(aBaseAddr, 32)
        Set xform_rec22_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w0)(aBaseAddr, 32)
        Set xform_rec22_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w1)(aBaseAddr, 32)
        Set xform_rec22_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w2)(aBaseAddr, 32)
        Set xform_rec22_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w3)(aBaseAddr, 32)
        Set xform_rec22_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w4)(aBaseAddr, 32)
        Set xform_rec22_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w5)(aBaseAddr, 32)
        Set xform_rec22_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w6)(aBaseAddr, 32)
        Set xform_rec22_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w7)(aBaseAddr, 32)
        Set xform_rec22_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w8)(aBaseAddr, 32)
        Set xform_rec22_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w9)(aBaseAddr, 32)
        Set xform_rec22_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w10)(aBaseAddr, 32)
        Set xform_rec22_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w11)(aBaseAddr, 32)
        Set xform_rec22_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w12)(aBaseAddr, 32)
        Set xform_rec22_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w13)(aBaseAddr, 32)
        Set xform_rec22_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w14)(aBaseAddr, 32)
        Set xform_rec22_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w15)(aBaseAddr, 32)
        Set xform_rec22_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w16)(aBaseAddr, 32)
        Set xform_rec22_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w17)(aBaseAddr, 32)
        Set xform_rec22_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w18)(aBaseAddr, 32)
        Set xform_rec22_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w19)(aBaseAddr, 32)
        Set xform_rec22_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w20)(aBaseAddr, 32)
        Set xform_rec22_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w21)(aBaseAddr, 32)
        Set xform_rec22_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w22)(aBaseAddr, 32)
        Set xform_rec22_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w23)(aBaseAddr, 32)
        Set xform_rec23_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w0)(aBaseAddr, 32)
        Set xform_rec23_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w1)(aBaseAddr, 32)
        Set xform_rec23_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w2)(aBaseAddr, 32)
        Set xform_rec23_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w3)(aBaseAddr, 32)
        Set xform_rec23_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w4)(aBaseAddr, 32)
        Set xform_rec23_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w5)(aBaseAddr, 32)
        Set xform_rec23_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w6)(aBaseAddr, 32)
        Set xform_rec23_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w7)(aBaseAddr, 32)
        Set xform_rec23_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w8)(aBaseAddr, 32)
        Set xform_rec23_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w9)(aBaseAddr, 32)
        Set xform_rec23_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w10)(aBaseAddr, 32)
        Set xform_rec23_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w11)(aBaseAddr, 32)
        Set xform_rec23_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w12)(aBaseAddr, 32)
        Set xform_rec23_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w13)(aBaseAddr, 32)
        Set xform_rec23_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w14)(aBaseAddr, 32)
        Set xform_rec23_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w15)(aBaseAddr, 32)
        Set xform_rec23_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w16)(aBaseAddr, 32)
        Set xform_rec23_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w17)(aBaseAddr, 32)
        Set xform_rec23_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w18)(aBaseAddr, 32)
        Set xform_rec23_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w19)(aBaseAddr, 32)
        Set xform_rec23_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w20)(aBaseAddr, 32)
        Set xform_rec23_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w21)(aBaseAddr, 32)
        Set xform_rec23_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w22)(aBaseAddr, 32)
        Set xform_rec23_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w23)(aBaseAddr, 32)
        Set xform_rec24_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w0)(aBaseAddr, 32)
        Set xform_rec24_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w1)(aBaseAddr, 32)
        Set xform_rec24_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w2)(aBaseAddr, 32)
        Set xform_rec24_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w3)(aBaseAddr, 32)
        Set xform_rec24_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w4)(aBaseAddr, 32)
        Set xform_rec24_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w5)(aBaseAddr, 32)
        Set xform_rec24_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w6)(aBaseAddr, 32)
        Set xform_rec24_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w7)(aBaseAddr, 32)
        Set xform_rec24_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w8)(aBaseAddr, 32)
        Set xform_rec24_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w9)(aBaseAddr, 32)
        Set xform_rec24_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w10)(aBaseAddr, 32)
        Set xform_rec24_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w11)(aBaseAddr, 32)
        Set xform_rec24_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w12)(aBaseAddr, 32)
        Set xform_rec24_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w13)(aBaseAddr, 32)
        Set xform_rec24_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w14)(aBaseAddr, 32)
        Set xform_rec24_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w15)(aBaseAddr, 32)
        Set xform_rec24_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w16)(aBaseAddr, 32)
        Set xform_rec24_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w17)(aBaseAddr, 32)
        Set xform_rec24_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w18)(aBaseAddr, 32)
        Set xform_rec24_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w19)(aBaseAddr, 32)
        Set xform_rec24_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w20)(aBaseAddr, 32)
        Set xform_rec24_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w21)(aBaseAddr, 32)
        Set xform_rec24_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w22)(aBaseAddr, 32)
        Set xform_rec24_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w23)(aBaseAddr, 32)
        Set xform_rec25_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w0)(aBaseAddr, 32)
        Set xform_rec25_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w1)(aBaseAddr, 32)
        Set xform_rec25_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w2)(aBaseAddr, 32)
        Set xform_rec25_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w3)(aBaseAddr, 32)
        Set xform_rec25_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w4)(aBaseAddr, 32)
        Set xform_rec25_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w5)(aBaseAddr, 32)
        Set xform_rec25_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w6)(aBaseAddr, 32)
        Set xform_rec25_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w7)(aBaseAddr, 32)
        Set xform_rec25_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w8)(aBaseAddr, 32)
        Set xform_rec25_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w9)(aBaseAddr, 32)
        Set xform_rec25_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w10)(aBaseAddr, 32)
        Set xform_rec25_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w11)(aBaseAddr, 32)
        Set xform_rec25_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w12)(aBaseAddr, 32)
        Set xform_rec25_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w13)(aBaseAddr, 32)
        Set xform_rec25_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w14)(aBaseAddr, 32)
        Set xform_rec25_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w15)(aBaseAddr, 32)
        Set xform_rec25_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w16)(aBaseAddr, 32)
        Set xform_rec25_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w17)(aBaseAddr, 32)
        Set xform_rec25_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w18)(aBaseAddr, 32)
        Set xform_rec25_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w19)(aBaseAddr, 32)
        Set xform_rec25_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w20)(aBaseAddr, 32)
        Set xform_rec25_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w21)(aBaseAddr, 32)
        Set xform_rec25_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w22)(aBaseAddr, 32)
        Set xform_rec25_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w23)(aBaseAddr, 32)
        Set xform_rec26_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w0)(aBaseAddr, 32)
        Set xform_rec26_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w1)(aBaseAddr, 32)
        Set xform_rec26_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w2)(aBaseAddr, 32)
        Set xform_rec26_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w3)(aBaseAddr, 32)
        Set xform_rec26_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w4)(aBaseAddr, 32)
        Set xform_rec26_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w5)(aBaseAddr, 32)
        Set xform_rec26_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w6)(aBaseAddr, 32)
        Set xform_rec26_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w7)(aBaseAddr, 32)
        Set xform_rec26_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w8)(aBaseAddr, 32)
        Set xform_rec26_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w9)(aBaseAddr, 32)
        Set xform_rec26_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w10)(aBaseAddr, 32)
        Set xform_rec26_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w11)(aBaseAddr, 32)
        Set xform_rec26_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w12)(aBaseAddr, 32)
        Set xform_rec26_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w13)(aBaseAddr, 32)
        Set xform_rec26_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w14)(aBaseAddr, 32)
        Set xform_rec26_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w15)(aBaseAddr, 32)
        Set xform_rec26_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w16)(aBaseAddr, 32)
        Set xform_rec26_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w17)(aBaseAddr, 32)
        Set xform_rec26_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w18)(aBaseAddr, 32)
        Set xform_rec26_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w19)(aBaseAddr, 32)
        Set xform_rec26_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w20)(aBaseAddr, 32)
        Set xform_rec26_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w21)(aBaseAddr, 32)
        Set xform_rec26_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w22)(aBaseAddr, 32)
        Set xform_rec26_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w23)(aBaseAddr, 32)
        Set xform_rec27_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w0)(aBaseAddr, 32)
        Set xform_rec27_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w1)(aBaseAddr, 32)
        Set xform_rec27_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w2)(aBaseAddr, 32)
        Set xform_rec27_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w3)(aBaseAddr, 32)
        Set xform_rec27_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w4)(aBaseAddr, 32)
        Set xform_rec27_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w5)(aBaseAddr, 32)
        Set xform_rec27_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w6)(aBaseAddr, 32)
        Set xform_rec27_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w7)(aBaseAddr, 32)
        Set xform_rec27_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w8)(aBaseAddr, 32)
        Set xform_rec27_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w9)(aBaseAddr, 32)
        Set xform_rec27_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w10)(aBaseAddr, 32)
        Set xform_rec27_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w11)(aBaseAddr, 32)
        Set xform_rec27_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w12)(aBaseAddr, 32)
        Set xform_rec27_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w13)(aBaseAddr, 32)
        Set xform_rec27_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w14)(aBaseAddr, 32)
        Set xform_rec27_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w15)(aBaseAddr, 32)
        Set xform_rec27_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w16)(aBaseAddr, 32)
        Set xform_rec27_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w17)(aBaseAddr, 32)
        Set xform_rec27_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w18)(aBaseAddr, 32)
        Set xform_rec27_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w19)(aBaseAddr, 32)
        Set xform_rec27_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w20)(aBaseAddr, 32)
        Set xform_rec27_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w21)(aBaseAddr, 32)
        Set xform_rec27_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w22)(aBaseAddr, 32)
        Set xform_rec27_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w23)(aBaseAddr, 32)
        Set xform_rec28_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w0)(aBaseAddr, 32)
        Set xform_rec28_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w1)(aBaseAddr, 32)
        Set xform_rec28_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w2)(aBaseAddr, 32)
        Set xform_rec28_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w3)(aBaseAddr, 32)
        Set xform_rec28_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w4)(aBaseAddr, 32)
        Set xform_rec28_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w5)(aBaseAddr, 32)
        Set xform_rec28_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w6)(aBaseAddr, 32)
        Set xform_rec28_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w7)(aBaseAddr, 32)
        Set xform_rec28_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w8)(aBaseAddr, 32)
        Set xform_rec28_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w9)(aBaseAddr, 32)
        Set xform_rec28_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w10)(aBaseAddr, 32)
        Set xform_rec28_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w11)(aBaseAddr, 32)
        Set xform_rec28_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w12)(aBaseAddr, 32)
        Set xform_rec28_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w13)(aBaseAddr, 32)
        Set xform_rec28_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w14)(aBaseAddr, 32)
        Set xform_rec28_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w15)(aBaseAddr, 32)
        Set xform_rec28_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w16)(aBaseAddr, 32)
        Set xform_rec28_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w17)(aBaseAddr, 32)
        Set xform_rec28_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w18)(aBaseAddr, 32)
        Set xform_rec28_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w19)(aBaseAddr, 32)
        Set xform_rec28_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w20)(aBaseAddr, 32)
        Set xform_rec28_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w21)(aBaseAddr, 32)
        Set xform_rec28_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w22)(aBaseAddr, 32)
        Set xform_rec28_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w23)(aBaseAddr, 32)
        Set xform_rec29_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w0)(aBaseAddr, 32)
        Set xform_rec29_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w1)(aBaseAddr, 32)
        Set xform_rec29_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w2)(aBaseAddr, 32)
        Set xform_rec29_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w3)(aBaseAddr, 32)
        Set xform_rec29_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w4)(aBaseAddr, 32)
        Set xform_rec29_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w5)(aBaseAddr, 32)
        Set xform_rec29_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w6)(aBaseAddr, 32)
        Set xform_rec29_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w7)(aBaseAddr, 32)
        Set xform_rec29_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w8)(aBaseAddr, 32)
        Set xform_rec29_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w9)(aBaseAddr, 32)
        Set xform_rec29_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w10)(aBaseAddr, 32)
        Set xform_rec29_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w11)(aBaseAddr, 32)
        Set xform_rec29_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w12)(aBaseAddr, 32)
        Set xform_rec29_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w13)(aBaseAddr, 32)
        Set xform_rec29_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w14)(aBaseAddr, 32)
        Set xform_rec29_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w15)(aBaseAddr, 32)
        Set xform_rec29_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w16)(aBaseAddr, 32)
        Set xform_rec29_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w17)(aBaseAddr, 32)
        Set xform_rec29_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w18)(aBaseAddr, 32)
        Set xform_rec29_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w19)(aBaseAddr, 32)
        Set xform_rec29_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w20)(aBaseAddr, 32)
        Set xform_rec29_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w21)(aBaseAddr, 32)
        Set xform_rec29_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w22)(aBaseAddr, 32)
        Set xform_rec29_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w23)(aBaseAddr, 32)
        Set xform_rec30_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w0)(aBaseAddr, 32)
        Set xform_rec30_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w1)(aBaseAddr, 32)
        Set xform_rec30_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w2)(aBaseAddr, 32)
        Set xform_rec30_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w3)(aBaseAddr, 32)
        Set xform_rec30_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w4)(aBaseAddr, 32)
        Set xform_rec30_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w5)(aBaseAddr, 32)
        Set xform_rec30_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w6)(aBaseAddr, 32)
        Set xform_rec30_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w7)(aBaseAddr, 32)
        Set xform_rec30_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w8)(aBaseAddr, 32)
        Set xform_rec30_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w9)(aBaseAddr, 32)
        Set xform_rec30_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w10)(aBaseAddr, 32)
        Set xform_rec30_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w11)(aBaseAddr, 32)
        Set xform_rec30_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w12)(aBaseAddr, 32)
        Set xform_rec30_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w13)(aBaseAddr, 32)
        Set xform_rec30_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w14)(aBaseAddr, 32)
        Set xform_rec30_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w15)(aBaseAddr, 32)
        Set xform_rec30_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w16)(aBaseAddr, 32)
        Set xform_rec30_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w17)(aBaseAddr, 32)
        Set xform_rec30_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w18)(aBaseAddr, 32)
        Set xform_rec30_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w19)(aBaseAddr, 32)
        Set xform_rec30_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w20)(aBaseAddr, 32)
        Set xform_rec30_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w21)(aBaseAddr, 32)
        Set xform_rec30_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w22)(aBaseAddr, 32)
        Set xform_rec30_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w23)(aBaseAddr, 32)
        Set xform_rec31_w0 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w0)(aBaseAddr, 32)
        Set xform_rec31_w1 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w1)(aBaseAddr, 32)
        Set xform_rec31_w2 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w2)(aBaseAddr, 32)
        Set xform_rec31_w3 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w3)(aBaseAddr, 32)
        Set xform_rec31_w4 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w4)(aBaseAddr, 32)
        Set xform_rec31_w5 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w5)(aBaseAddr, 32)
        Set xform_rec31_w6 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w6)(aBaseAddr, 32)
        Set xform_rec31_w7 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w7)(aBaseAddr, 32)
        Set xform_rec31_w8 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w8)(aBaseAddr, 32)
        Set xform_rec31_w9 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w9)(aBaseAddr, 32)
        Set xform_rec31_w10 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w10)(aBaseAddr, 32)
        Set xform_rec31_w11 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w11)(aBaseAddr, 32)
        Set xform_rec31_w12 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w12)(aBaseAddr, 32)
        Set xform_rec31_w13 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w13)(aBaseAddr, 32)
        Set xform_rec31_w14 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w14)(aBaseAddr, 32)
        Set xform_rec31_w15 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w15)(aBaseAddr, 32)
        Set xform_rec31_w16 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w16)(aBaseAddr, 32)
        Set xform_rec31_w17 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w17)(aBaseAddr, 32)
        Set xform_rec31_w18 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w18)(aBaseAddr, 32)
        Set xform_rec31_w19 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w19)(aBaseAddr, 32)
        Set xform_rec31_w20 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w20)(aBaseAddr, 32)
        Set xform_rec31_w21 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w21)(aBaseAddr, 32)
        Set xform_rec31_w22 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w22)(aBaseAddr, 32)
        Set xform_rec31_w23 = (New REGISTER_EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w23)(aBaseAddr, 32)

        Set Init = Me
    end function

End Class


'***********************************

' Regsets

'***********************************


Set EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS = CreateObject("System.Collections.ArrayList")
EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS.Add ((New EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_INSTANCE)(&H4d010000))
EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS.Add ((New EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_INSTANCE)(&H4d110000))
EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS.Add ((New EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_INSTANCE)(&H4d210000))
EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS.Add ((New EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_INSTANCE)(&H4d310000))
EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS.Add ((New EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_INSTANCE)(&H4d410000))
EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS.Add ((New EIP165S_EIP_160S_I_32_BRCM_TRANSFORM_RECORDS_INSTANCE)(&H4d510000))


