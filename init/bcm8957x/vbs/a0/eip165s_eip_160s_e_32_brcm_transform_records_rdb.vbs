

Include("helper_functions.vbs")

Sub Include (strFile)
    'Create objects for opening text file
    Set objFSO = CreateObject("Scripting.FileSystemObject")
    Set objTextFile = objFSO.OpenTextFile(strFile, 1)

    'Execute content of file.
    ExecuteGlobal objTextFile.ReadAll

    'CLose file
    objTextFile.Close

    'Clean up
    Set objFSO = Nothing
    Set objTextFile = Nothing
End Sub


'***********************************

' Registers and Fields

'***********************************


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10000
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10004
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10008
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1000c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10010
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10014
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10018
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1001c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10020
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10024
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10028
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1002c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10030
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10034
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10038
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1003c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10040
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10044
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10048
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1004c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10050
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10054
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10058
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1005c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10080
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10084
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10088
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1008c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10090
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10094
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10098
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1009c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H100a0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H100a4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H100a8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H100ac
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H100b0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H100b4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H100b8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H100bc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H100c0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H100c4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H100c8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H100cc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H100d0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H100d4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H100d8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H100dc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10100
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10104
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10108
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1010c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10110
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10114
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10118
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1011c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10120
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10124
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10128
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1012c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10130
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10134
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10138
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1013c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10140
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10144
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10148
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1014c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10150
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10154
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10158
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1015c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10180
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10184
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10188
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1018c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10190
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10194
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10198
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1019c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H101a0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H101a4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H101a8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H101ac
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H101b0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H101b4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H101b8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H101bc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H101c0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H101c4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H101c8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H101cc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H101d0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H101d4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H101d8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H101dc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10200
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10204
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10208
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1020c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10210
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10214
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10218
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1021c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10220
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10224
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10228
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1022c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10230
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10234
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10238
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1023c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10240
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10244
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10248
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1024c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10250
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10254
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10258
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1025c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10280
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10284
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10288
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1028c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10290
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10294
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10298
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1029c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H102a0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H102a4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H102a8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H102ac
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H102b0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H102b4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H102b8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H102bc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H102c0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H102c4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H102c8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H102cc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H102d0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H102d4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H102d8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H102dc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10300
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10304
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10308
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1030c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10310
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10314
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10318
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1031c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10320
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10324
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10328
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1032c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10330
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10334
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10338
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1033c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10340
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10344
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10348
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1034c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10350
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10354
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10358
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1035c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10380
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10384
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10388
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1038c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10390
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10394
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10398
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1039c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H103a0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H103a4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H103a8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H103ac
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H103b0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H103b4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H103b8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H103bc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H103c0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H103c4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H103c8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H103cc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H103d0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H103d4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H103d8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H103dc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10400
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10404
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10408
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1040c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10410
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10414
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10418
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1041c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10420
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10424
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10428
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1042c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10430
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10434
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10438
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1043c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10440
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10444
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10448
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1044c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10450
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10454
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10458
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1045c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10480
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10484
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10488
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1048c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10490
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10494
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10498
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1049c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H104a0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H104a4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H104a8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H104ac
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H104b0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H104b4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H104b8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H104bc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H104c0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H104c4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H104c8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H104cc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H104d0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H104d4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H104d8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H104dc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10500
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10504
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10508
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1050c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10510
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10514
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10518
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1051c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10520
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10524
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10528
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1052c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10530
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10534
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10538
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1053c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10540
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10544
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10548
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1054c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10550
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10554
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10558
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1055c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10580
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10584
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10588
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1058c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10590
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10594
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10598
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1059c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H105a0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H105a4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H105a8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H105ac
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H105b0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H105b4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H105b8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H105bc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H105c0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H105c4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H105c8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H105cc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H105d0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H105d4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H105d8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H105dc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10600
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10604
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10608
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1060c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10610
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10614
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10618
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1061c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10620
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10624
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10628
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1062c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10630
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10634
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10638
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1063c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10640
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10644
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10648
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1064c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10650
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10654
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10658
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1065c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10680
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10684
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10688
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1068c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10690
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10694
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10698
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1069c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H106a0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H106a4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H106a8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H106ac
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H106b0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H106b4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H106b8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H106bc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H106c0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H106c4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H106c8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H106cc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H106d0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H106d4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H106d8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H106dc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10700
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10704
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10708
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1070c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10710
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10714
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10718
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1071c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10720
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10724
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10728
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1072c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10730
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10734
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10738
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1073c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10740
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10744
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10748
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1074c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10750
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10754
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10758
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1075c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10780
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10784
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10788
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1078c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10790
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10794
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10798
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1079c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H107a0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H107a4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H107a8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H107ac
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H107b0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H107b4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H107b8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H107bc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H107c0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H107c4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H107c8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H107cc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H107d0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H107d4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H107d8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H107dc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10800
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10804
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10808
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1080c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10810
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10814
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10818
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1081c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10820
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10824
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10828
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1082c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10830
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10834
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10838
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1083c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10840
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10844
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10848
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1084c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10850
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10854
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10858
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1085c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10880
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10884
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10888
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1088c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10890
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10894
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10898
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1089c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H108a0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H108a4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H108a8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H108ac
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H108b0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H108b4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H108b8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H108bc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H108c0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H108c4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H108c8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H108cc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H108d0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H108d4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H108d8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H108dc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10900
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10904
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10908
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1090c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10910
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10914
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10918
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1091c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10920
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10924
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10928
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1092c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10930
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10934
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10938
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1093c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10940
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10944
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10948
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1094c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10950
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10954
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10958
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1095c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10980
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10984
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10988
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1098c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10990
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10994
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10998
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H1099c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H109a0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H109a4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H109a8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H109ac
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H109b0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H109b4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H109b8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H109bc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H109c0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H109c4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H109c8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H109cc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H109d0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H109d4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H109d8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H109dc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a00
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a04
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a08
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a0c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a10
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a14
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a18
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a1c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a20
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a24
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a28
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a2c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a30
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a34
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a38
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a3c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a40
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a44
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a48
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a4c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a50
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a54
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a58
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a5c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a80
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a84
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a88
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a8c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a90
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a94
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a98
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10a9c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10aa0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10aa4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10aa8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10aac
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ab0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ab4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ab8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10abc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ac0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ac4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ac8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10acc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ad0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ad4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ad8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10adc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b00
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b04
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b08
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b0c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b10
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b14
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b18
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b1c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b20
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b24
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b28
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b2c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b30
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b34
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b38
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b3c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b40
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b44
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b48
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b4c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b50
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b54
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b58
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b5c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b80
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b84
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b88
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b8c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b90
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b94
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b98
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10b9c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ba0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ba4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ba8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10bac
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10bb0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10bb4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10bb8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10bbc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10bc0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10bc4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10bc8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10bcc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10bd0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10bd4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10bd8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10bdc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c00
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c04
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c08
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c0c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c10
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c14
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c18
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c1c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c20
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c24
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c28
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c2c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c30
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c34
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c38
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c3c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c40
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c44
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c48
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c4c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c50
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c54
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c58
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c5c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c80
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c84
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c88
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c8c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c90
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c94
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c98
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10c9c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ca0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ca4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ca8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10cac
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10cb0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10cb4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10cb8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10cbc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10cc0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10cc4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10cc8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ccc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10cd0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10cd4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10cd8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10cdc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d00
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d04
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d08
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d0c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d10
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d14
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d18
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d1c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d20
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d24
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d28
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d2c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d30
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d34
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d38
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d3c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d40
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d44
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d48
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d4c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d50
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d54
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d58
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d5c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d80
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d84
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d88
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d8c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d90
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d94
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d98
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10d9c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10da0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10da4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10da8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10dac
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10db0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10db4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10db8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10dbc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10dc0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10dc4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10dc8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10dcc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10dd0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10dd4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10dd8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ddc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e00
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e04
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e08
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e0c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e10
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e14
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e18
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e1c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e20
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e24
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e28
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e2c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e30
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e34
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e38
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e3c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e40
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e44
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e48
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e4c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e50
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e54
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e58
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e5c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e80
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e84
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e88
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e8c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e90
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e94
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e98
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10e9c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ea0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ea4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ea8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10eac
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10eb0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10eb4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10eb8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ebc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ec0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ec4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ec8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ecc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ed0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ed4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10ed8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10edc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f00
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f04
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f08
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f0c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f10
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f14
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f18
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f1c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f20
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f24
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f28
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f2c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f30
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f34
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f38
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f3c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f40
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f44
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f48
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f4c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f50
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f54
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f58
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f5c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w0
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w0
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f80
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w1
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w1
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f84
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w2
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w2
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f88
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w3
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w3
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f8c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w4
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w4
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f90
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w5
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w5
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f94
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w6
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w6
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f98
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w7
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w7
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10f9c
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w8
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w8
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10fa0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w9
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w9
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10fa4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w10
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w10
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10fa8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w11
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w11
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10fac
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w12
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w12
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10fb0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w13
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w13
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10fb4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w14
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w14
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10fb8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w15
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w15
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10fbc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w16
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w16
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10fc0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w17
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w17
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10fc4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w18
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w18
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10fc8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w19
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w19
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10fcc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w20
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w20
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10fd0
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w21
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w21
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10fd4
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w22
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w22
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10fd8
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class


'' @REGISTER : EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w23
'' @API      : data
''           : read
''           : write
''           : get_
''---------------------------------------------------------------------------------
'' @FIELDS                                    @RANGE           @API
''---------------------------------------------------------------------------------
'' transform_record_data                      [31:0]           get_transform_record_data
''                                                             set_transform_record_data
''                                                             read_transform_record_data
''                                                             write_transform_record_data
''---------------------------------------------------------------------------------
Class REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w23
    Private write_transform_record_data_value
    Private read_transform_record_data_value
    Private flag_transform_record_data

    Private offset
    Private address
    Private regSize
    Private regWidth
    Private regData
    Private mask

    Sub Class_Initialize
        offset = &H10fdc
    End Sub

    Public default function Init(aRegsetAddr, aSize)
        regSize  = aSize
        address  = aRegsetAddr + offset
        If regSize = 64 Then regWidth = 7 Else If regSize = 32 Then regWidth = 3 Else If regSize = 16 Then regWidth = 1 Else regWidth = 0
        If regSize = 8 Then mask = &Hff Else If regSize = 16 Then mask = &Hffff Else mask = &Hffffffff
        clear_all_fields
        Set Init = Me
    end function

    Property Get get_transform_record_data
        get_transform_record_data = read_transform_record_data_value
    End Property

    Property Let set_transform_record_data(aData)
        write_transform_record_data_value = aData
        flag_transform_record_data        = &H1
    End Property

    Property Get read_transform_record_data
        read
        read_transform_record_data = read_transform_record_data_value
    End Property

    Property Let write_transform_record_data(aData)
        set_transform_record_data = aData
        write
    End Property

    Property Get get_
        get_ = regData
    End Property

    Sub read
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

    End Sub

    Sub write
        If flag_transform_record_data = &H0 Then read
        If flag_transform_record_data = &H0 Then write_transform_record_data_value = get_transform_record_data

        regValue = leftShift(write_transform_record_data_value, 0)
        RegWrite regWidth, address, regValue
        clear_all_fields
    End Sub


    Property Get data
        data_low = RegRead(regWidth, address)
        data_low = Sys.bitand(data_low, mask)
        regData  = data_low
        transform_record_data_mask = &Hffffffff
        if data_low > LONG_MAX then
            if transform_record_data_mask = mask then
                read_transform_record_data_value = data_low
            else
                read_transform_record_data_value = (data_low - H8000_0000) and transform_record_data_mask
            end If
        else
            read_transform_record_data_value = data_low and transform_record_data_mask
        end If

        data = data_low
    End Property

    Property Let data(aValue)
        RegWrite regWidth, address, aValue
        clear_all_fields
    End Property

    Sub clear_all_fields
        write_transform_record_data_value = &H0
        flag_transform_record_data        = &H0
    End Sub
End Class




'***********************************

' Regset Instance

'***********************************

Class EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_INSTANCE

    Public xform_rec0_w0
    Public xform_rec0_w1
    Public xform_rec0_w2
    Public xform_rec0_w3
    Public xform_rec0_w4
    Public xform_rec0_w5
    Public xform_rec0_w6
    Public xform_rec0_w7
    Public xform_rec0_w8
    Public xform_rec0_w9
    Public xform_rec0_w10
    Public xform_rec0_w11
    Public xform_rec0_w12
    Public xform_rec0_w13
    Public xform_rec0_w14
    Public xform_rec0_w15
    Public xform_rec0_w16
    Public xform_rec0_w17
    Public xform_rec0_w18
    Public xform_rec0_w19
    Public xform_rec0_w20
    Public xform_rec0_w21
    Public xform_rec0_w22
    Public xform_rec0_w23
    Public xform_rec1_w0
    Public xform_rec1_w1
    Public xform_rec1_w2
    Public xform_rec1_w3
    Public xform_rec1_w4
    Public xform_rec1_w5
    Public xform_rec1_w6
    Public xform_rec1_w7
    Public xform_rec1_w8
    Public xform_rec1_w9
    Public xform_rec1_w10
    Public xform_rec1_w11
    Public xform_rec1_w12
    Public xform_rec1_w13
    Public xform_rec1_w14
    Public xform_rec1_w15
    Public xform_rec1_w16
    Public xform_rec1_w17
    Public xform_rec1_w18
    Public xform_rec1_w19
    Public xform_rec1_w20
    Public xform_rec1_w21
    Public xform_rec1_w22
    Public xform_rec1_w23
    Public xform_rec2_w0
    Public xform_rec2_w1
    Public xform_rec2_w2
    Public xform_rec2_w3
    Public xform_rec2_w4
    Public xform_rec2_w5
    Public xform_rec2_w6
    Public xform_rec2_w7
    Public xform_rec2_w8
    Public xform_rec2_w9
    Public xform_rec2_w10
    Public xform_rec2_w11
    Public xform_rec2_w12
    Public xform_rec2_w13
    Public xform_rec2_w14
    Public xform_rec2_w15
    Public xform_rec2_w16
    Public xform_rec2_w17
    Public xform_rec2_w18
    Public xform_rec2_w19
    Public xform_rec2_w20
    Public xform_rec2_w21
    Public xform_rec2_w22
    Public xform_rec2_w23
    Public xform_rec3_w0
    Public xform_rec3_w1
    Public xform_rec3_w2
    Public xform_rec3_w3
    Public xform_rec3_w4
    Public xform_rec3_w5
    Public xform_rec3_w6
    Public xform_rec3_w7
    Public xform_rec3_w8
    Public xform_rec3_w9
    Public xform_rec3_w10
    Public xform_rec3_w11
    Public xform_rec3_w12
    Public xform_rec3_w13
    Public xform_rec3_w14
    Public xform_rec3_w15
    Public xform_rec3_w16
    Public xform_rec3_w17
    Public xform_rec3_w18
    Public xform_rec3_w19
    Public xform_rec3_w20
    Public xform_rec3_w21
    Public xform_rec3_w22
    Public xform_rec3_w23
    Public xform_rec4_w0
    Public xform_rec4_w1
    Public xform_rec4_w2
    Public xform_rec4_w3
    Public xform_rec4_w4
    Public xform_rec4_w5
    Public xform_rec4_w6
    Public xform_rec4_w7
    Public xform_rec4_w8
    Public xform_rec4_w9
    Public xform_rec4_w10
    Public xform_rec4_w11
    Public xform_rec4_w12
    Public xform_rec4_w13
    Public xform_rec4_w14
    Public xform_rec4_w15
    Public xform_rec4_w16
    Public xform_rec4_w17
    Public xform_rec4_w18
    Public xform_rec4_w19
    Public xform_rec4_w20
    Public xform_rec4_w21
    Public xform_rec4_w22
    Public xform_rec4_w23
    Public xform_rec5_w0
    Public xform_rec5_w1
    Public xform_rec5_w2
    Public xform_rec5_w3
    Public xform_rec5_w4
    Public xform_rec5_w5
    Public xform_rec5_w6
    Public xform_rec5_w7
    Public xform_rec5_w8
    Public xform_rec5_w9
    Public xform_rec5_w10
    Public xform_rec5_w11
    Public xform_rec5_w12
    Public xform_rec5_w13
    Public xform_rec5_w14
    Public xform_rec5_w15
    Public xform_rec5_w16
    Public xform_rec5_w17
    Public xform_rec5_w18
    Public xform_rec5_w19
    Public xform_rec5_w20
    Public xform_rec5_w21
    Public xform_rec5_w22
    Public xform_rec5_w23
    Public xform_rec6_w0
    Public xform_rec6_w1
    Public xform_rec6_w2
    Public xform_rec6_w3
    Public xform_rec6_w4
    Public xform_rec6_w5
    Public xform_rec6_w6
    Public xform_rec6_w7
    Public xform_rec6_w8
    Public xform_rec6_w9
    Public xform_rec6_w10
    Public xform_rec6_w11
    Public xform_rec6_w12
    Public xform_rec6_w13
    Public xform_rec6_w14
    Public xform_rec6_w15
    Public xform_rec6_w16
    Public xform_rec6_w17
    Public xform_rec6_w18
    Public xform_rec6_w19
    Public xform_rec6_w20
    Public xform_rec6_w21
    Public xform_rec6_w22
    Public xform_rec6_w23
    Public xform_rec7_w0
    Public xform_rec7_w1
    Public xform_rec7_w2
    Public xform_rec7_w3
    Public xform_rec7_w4
    Public xform_rec7_w5
    Public xform_rec7_w6
    Public xform_rec7_w7
    Public xform_rec7_w8
    Public xform_rec7_w9
    Public xform_rec7_w10
    Public xform_rec7_w11
    Public xform_rec7_w12
    Public xform_rec7_w13
    Public xform_rec7_w14
    Public xform_rec7_w15
    Public xform_rec7_w16
    Public xform_rec7_w17
    Public xform_rec7_w18
    Public xform_rec7_w19
    Public xform_rec7_w20
    Public xform_rec7_w21
    Public xform_rec7_w22
    Public xform_rec7_w23
    Public xform_rec8_w0
    Public xform_rec8_w1
    Public xform_rec8_w2
    Public xform_rec8_w3
    Public xform_rec8_w4
    Public xform_rec8_w5
    Public xform_rec8_w6
    Public xform_rec8_w7
    Public xform_rec8_w8
    Public xform_rec8_w9
    Public xform_rec8_w10
    Public xform_rec8_w11
    Public xform_rec8_w12
    Public xform_rec8_w13
    Public xform_rec8_w14
    Public xform_rec8_w15
    Public xform_rec8_w16
    Public xform_rec8_w17
    Public xform_rec8_w18
    Public xform_rec8_w19
    Public xform_rec8_w20
    Public xform_rec8_w21
    Public xform_rec8_w22
    Public xform_rec8_w23
    Public xform_rec9_w0
    Public xform_rec9_w1
    Public xform_rec9_w2
    Public xform_rec9_w3
    Public xform_rec9_w4
    Public xform_rec9_w5
    Public xform_rec9_w6
    Public xform_rec9_w7
    Public xform_rec9_w8
    Public xform_rec9_w9
    Public xform_rec9_w10
    Public xform_rec9_w11
    Public xform_rec9_w12
    Public xform_rec9_w13
    Public xform_rec9_w14
    Public xform_rec9_w15
    Public xform_rec9_w16
    Public xform_rec9_w17
    Public xform_rec9_w18
    Public xform_rec9_w19
    Public xform_rec9_w20
    Public xform_rec9_w21
    Public xform_rec9_w22
    Public xform_rec9_w23
    Public xform_rec10_w0
    Public xform_rec10_w1
    Public xform_rec10_w2
    Public xform_rec10_w3
    Public xform_rec10_w4
    Public xform_rec10_w5
    Public xform_rec10_w6
    Public xform_rec10_w7
    Public xform_rec10_w8
    Public xform_rec10_w9
    Public xform_rec10_w10
    Public xform_rec10_w11
    Public xform_rec10_w12
    Public xform_rec10_w13
    Public xform_rec10_w14
    Public xform_rec10_w15
    Public xform_rec10_w16
    Public xform_rec10_w17
    Public xform_rec10_w18
    Public xform_rec10_w19
    Public xform_rec10_w20
    Public xform_rec10_w21
    Public xform_rec10_w22
    Public xform_rec10_w23
    Public xform_rec11_w0
    Public xform_rec11_w1
    Public xform_rec11_w2
    Public xform_rec11_w3
    Public xform_rec11_w4
    Public xform_rec11_w5
    Public xform_rec11_w6
    Public xform_rec11_w7
    Public xform_rec11_w8
    Public xform_rec11_w9
    Public xform_rec11_w10
    Public xform_rec11_w11
    Public xform_rec11_w12
    Public xform_rec11_w13
    Public xform_rec11_w14
    Public xform_rec11_w15
    Public xform_rec11_w16
    Public xform_rec11_w17
    Public xform_rec11_w18
    Public xform_rec11_w19
    Public xform_rec11_w20
    Public xform_rec11_w21
    Public xform_rec11_w22
    Public xform_rec11_w23
    Public xform_rec12_w0
    Public xform_rec12_w1
    Public xform_rec12_w2
    Public xform_rec12_w3
    Public xform_rec12_w4
    Public xform_rec12_w5
    Public xform_rec12_w6
    Public xform_rec12_w7
    Public xform_rec12_w8
    Public xform_rec12_w9
    Public xform_rec12_w10
    Public xform_rec12_w11
    Public xform_rec12_w12
    Public xform_rec12_w13
    Public xform_rec12_w14
    Public xform_rec12_w15
    Public xform_rec12_w16
    Public xform_rec12_w17
    Public xform_rec12_w18
    Public xform_rec12_w19
    Public xform_rec12_w20
    Public xform_rec12_w21
    Public xform_rec12_w22
    Public xform_rec12_w23
    Public xform_rec13_w0
    Public xform_rec13_w1
    Public xform_rec13_w2
    Public xform_rec13_w3
    Public xform_rec13_w4
    Public xform_rec13_w5
    Public xform_rec13_w6
    Public xform_rec13_w7
    Public xform_rec13_w8
    Public xform_rec13_w9
    Public xform_rec13_w10
    Public xform_rec13_w11
    Public xform_rec13_w12
    Public xform_rec13_w13
    Public xform_rec13_w14
    Public xform_rec13_w15
    Public xform_rec13_w16
    Public xform_rec13_w17
    Public xform_rec13_w18
    Public xform_rec13_w19
    Public xform_rec13_w20
    Public xform_rec13_w21
    Public xform_rec13_w22
    Public xform_rec13_w23
    Public xform_rec14_w0
    Public xform_rec14_w1
    Public xform_rec14_w2
    Public xform_rec14_w3
    Public xform_rec14_w4
    Public xform_rec14_w5
    Public xform_rec14_w6
    Public xform_rec14_w7
    Public xform_rec14_w8
    Public xform_rec14_w9
    Public xform_rec14_w10
    Public xform_rec14_w11
    Public xform_rec14_w12
    Public xform_rec14_w13
    Public xform_rec14_w14
    Public xform_rec14_w15
    Public xform_rec14_w16
    Public xform_rec14_w17
    Public xform_rec14_w18
    Public xform_rec14_w19
    Public xform_rec14_w20
    Public xform_rec14_w21
    Public xform_rec14_w22
    Public xform_rec14_w23
    Public xform_rec15_w0
    Public xform_rec15_w1
    Public xform_rec15_w2
    Public xform_rec15_w3
    Public xform_rec15_w4
    Public xform_rec15_w5
    Public xform_rec15_w6
    Public xform_rec15_w7
    Public xform_rec15_w8
    Public xform_rec15_w9
    Public xform_rec15_w10
    Public xform_rec15_w11
    Public xform_rec15_w12
    Public xform_rec15_w13
    Public xform_rec15_w14
    Public xform_rec15_w15
    Public xform_rec15_w16
    Public xform_rec15_w17
    Public xform_rec15_w18
    Public xform_rec15_w19
    Public xform_rec15_w20
    Public xform_rec15_w21
    Public xform_rec15_w22
    Public xform_rec15_w23
    Public xform_rec16_w0
    Public xform_rec16_w1
    Public xform_rec16_w2
    Public xform_rec16_w3
    Public xform_rec16_w4
    Public xform_rec16_w5
    Public xform_rec16_w6
    Public xform_rec16_w7
    Public xform_rec16_w8
    Public xform_rec16_w9
    Public xform_rec16_w10
    Public xform_rec16_w11
    Public xform_rec16_w12
    Public xform_rec16_w13
    Public xform_rec16_w14
    Public xform_rec16_w15
    Public xform_rec16_w16
    Public xform_rec16_w17
    Public xform_rec16_w18
    Public xform_rec16_w19
    Public xform_rec16_w20
    Public xform_rec16_w21
    Public xform_rec16_w22
    Public xform_rec16_w23
    Public xform_rec17_w0
    Public xform_rec17_w1
    Public xform_rec17_w2
    Public xform_rec17_w3
    Public xform_rec17_w4
    Public xform_rec17_w5
    Public xform_rec17_w6
    Public xform_rec17_w7
    Public xform_rec17_w8
    Public xform_rec17_w9
    Public xform_rec17_w10
    Public xform_rec17_w11
    Public xform_rec17_w12
    Public xform_rec17_w13
    Public xform_rec17_w14
    Public xform_rec17_w15
    Public xform_rec17_w16
    Public xform_rec17_w17
    Public xform_rec17_w18
    Public xform_rec17_w19
    Public xform_rec17_w20
    Public xform_rec17_w21
    Public xform_rec17_w22
    Public xform_rec17_w23
    Public xform_rec18_w0
    Public xform_rec18_w1
    Public xform_rec18_w2
    Public xform_rec18_w3
    Public xform_rec18_w4
    Public xform_rec18_w5
    Public xform_rec18_w6
    Public xform_rec18_w7
    Public xform_rec18_w8
    Public xform_rec18_w9
    Public xform_rec18_w10
    Public xform_rec18_w11
    Public xform_rec18_w12
    Public xform_rec18_w13
    Public xform_rec18_w14
    Public xform_rec18_w15
    Public xform_rec18_w16
    Public xform_rec18_w17
    Public xform_rec18_w18
    Public xform_rec18_w19
    Public xform_rec18_w20
    Public xform_rec18_w21
    Public xform_rec18_w22
    Public xform_rec18_w23
    Public xform_rec19_w0
    Public xform_rec19_w1
    Public xform_rec19_w2
    Public xform_rec19_w3
    Public xform_rec19_w4
    Public xform_rec19_w5
    Public xform_rec19_w6
    Public xform_rec19_w7
    Public xform_rec19_w8
    Public xform_rec19_w9
    Public xform_rec19_w10
    Public xform_rec19_w11
    Public xform_rec19_w12
    Public xform_rec19_w13
    Public xform_rec19_w14
    Public xform_rec19_w15
    Public xform_rec19_w16
    Public xform_rec19_w17
    Public xform_rec19_w18
    Public xform_rec19_w19
    Public xform_rec19_w20
    Public xform_rec19_w21
    Public xform_rec19_w22
    Public xform_rec19_w23
    Public xform_rec20_w0
    Public xform_rec20_w1
    Public xform_rec20_w2
    Public xform_rec20_w3
    Public xform_rec20_w4
    Public xform_rec20_w5
    Public xform_rec20_w6
    Public xform_rec20_w7
    Public xform_rec20_w8
    Public xform_rec20_w9
    Public xform_rec20_w10
    Public xform_rec20_w11
    Public xform_rec20_w12
    Public xform_rec20_w13
    Public xform_rec20_w14
    Public xform_rec20_w15
    Public xform_rec20_w16
    Public xform_rec20_w17
    Public xform_rec20_w18
    Public xform_rec20_w19
    Public xform_rec20_w20
    Public xform_rec20_w21
    Public xform_rec20_w22
    Public xform_rec20_w23
    Public xform_rec21_w0
    Public xform_rec21_w1
    Public xform_rec21_w2
    Public xform_rec21_w3
    Public xform_rec21_w4
    Public xform_rec21_w5
    Public xform_rec21_w6
    Public xform_rec21_w7
    Public xform_rec21_w8
    Public xform_rec21_w9
    Public xform_rec21_w10
    Public xform_rec21_w11
    Public xform_rec21_w12
    Public xform_rec21_w13
    Public xform_rec21_w14
    Public xform_rec21_w15
    Public xform_rec21_w16
    Public xform_rec21_w17
    Public xform_rec21_w18
    Public xform_rec21_w19
    Public xform_rec21_w20
    Public xform_rec21_w21
    Public xform_rec21_w22
    Public xform_rec21_w23
    Public xform_rec22_w0
    Public xform_rec22_w1
    Public xform_rec22_w2
    Public xform_rec22_w3
    Public xform_rec22_w4
    Public xform_rec22_w5
    Public xform_rec22_w6
    Public xform_rec22_w7
    Public xform_rec22_w8
    Public xform_rec22_w9
    Public xform_rec22_w10
    Public xform_rec22_w11
    Public xform_rec22_w12
    Public xform_rec22_w13
    Public xform_rec22_w14
    Public xform_rec22_w15
    Public xform_rec22_w16
    Public xform_rec22_w17
    Public xform_rec22_w18
    Public xform_rec22_w19
    Public xform_rec22_w20
    Public xform_rec22_w21
    Public xform_rec22_w22
    Public xform_rec22_w23
    Public xform_rec23_w0
    Public xform_rec23_w1
    Public xform_rec23_w2
    Public xform_rec23_w3
    Public xform_rec23_w4
    Public xform_rec23_w5
    Public xform_rec23_w6
    Public xform_rec23_w7
    Public xform_rec23_w8
    Public xform_rec23_w9
    Public xform_rec23_w10
    Public xform_rec23_w11
    Public xform_rec23_w12
    Public xform_rec23_w13
    Public xform_rec23_w14
    Public xform_rec23_w15
    Public xform_rec23_w16
    Public xform_rec23_w17
    Public xform_rec23_w18
    Public xform_rec23_w19
    Public xform_rec23_w20
    Public xform_rec23_w21
    Public xform_rec23_w22
    Public xform_rec23_w23
    Public xform_rec24_w0
    Public xform_rec24_w1
    Public xform_rec24_w2
    Public xform_rec24_w3
    Public xform_rec24_w4
    Public xform_rec24_w5
    Public xform_rec24_w6
    Public xform_rec24_w7
    Public xform_rec24_w8
    Public xform_rec24_w9
    Public xform_rec24_w10
    Public xform_rec24_w11
    Public xform_rec24_w12
    Public xform_rec24_w13
    Public xform_rec24_w14
    Public xform_rec24_w15
    Public xform_rec24_w16
    Public xform_rec24_w17
    Public xform_rec24_w18
    Public xform_rec24_w19
    Public xform_rec24_w20
    Public xform_rec24_w21
    Public xform_rec24_w22
    Public xform_rec24_w23
    Public xform_rec25_w0
    Public xform_rec25_w1
    Public xform_rec25_w2
    Public xform_rec25_w3
    Public xform_rec25_w4
    Public xform_rec25_w5
    Public xform_rec25_w6
    Public xform_rec25_w7
    Public xform_rec25_w8
    Public xform_rec25_w9
    Public xform_rec25_w10
    Public xform_rec25_w11
    Public xform_rec25_w12
    Public xform_rec25_w13
    Public xform_rec25_w14
    Public xform_rec25_w15
    Public xform_rec25_w16
    Public xform_rec25_w17
    Public xform_rec25_w18
    Public xform_rec25_w19
    Public xform_rec25_w20
    Public xform_rec25_w21
    Public xform_rec25_w22
    Public xform_rec25_w23
    Public xform_rec26_w0
    Public xform_rec26_w1
    Public xform_rec26_w2
    Public xform_rec26_w3
    Public xform_rec26_w4
    Public xform_rec26_w5
    Public xform_rec26_w6
    Public xform_rec26_w7
    Public xform_rec26_w8
    Public xform_rec26_w9
    Public xform_rec26_w10
    Public xform_rec26_w11
    Public xform_rec26_w12
    Public xform_rec26_w13
    Public xform_rec26_w14
    Public xform_rec26_w15
    Public xform_rec26_w16
    Public xform_rec26_w17
    Public xform_rec26_w18
    Public xform_rec26_w19
    Public xform_rec26_w20
    Public xform_rec26_w21
    Public xform_rec26_w22
    Public xform_rec26_w23
    Public xform_rec27_w0
    Public xform_rec27_w1
    Public xform_rec27_w2
    Public xform_rec27_w3
    Public xform_rec27_w4
    Public xform_rec27_w5
    Public xform_rec27_w6
    Public xform_rec27_w7
    Public xform_rec27_w8
    Public xform_rec27_w9
    Public xform_rec27_w10
    Public xform_rec27_w11
    Public xform_rec27_w12
    Public xform_rec27_w13
    Public xform_rec27_w14
    Public xform_rec27_w15
    Public xform_rec27_w16
    Public xform_rec27_w17
    Public xform_rec27_w18
    Public xform_rec27_w19
    Public xform_rec27_w20
    Public xform_rec27_w21
    Public xform_rec27_w22
    Public xform_rec27_w23
    Public xform_rec28_w0
    Public xform_rec28_w1
    Public xform_rec28_w2
    Public xform_rec28_w3
    Public xform_rec28_w4
    Public xform_rec28_w5
    Public xform_rec28_w6
    Public xform_rec28_w7
    Public xform_rec28_w8
    Public xform_rec28_w9
    Public xform_rec28_w10
    Public xform_rec28_w11
    Public xform_rec28_w12
    Public xform_rec28_w13
    Public xform_rec28_w14
    Public xform_rec28_w15
    Public xform_rec28_w16
    Public xform_rec28_w17
    Public xform_rec28_w18
    Public xform_rec28_w19
    Public xform_rec28_w20
    Public xform_rec28_w21
    Public xform_rec28_w22
    Public xform_rec28_w23
    Public xform_rec29_w0
    Public xform_rec29_w1
    Public xform_rec29_w2
    Public xform_rec29_w3
    Public xform_rec29_w4
    Public xform_rec29_w5
    Public xform_rec29_w6
    Public xform_rec29_w7
    Public xform_rec29_w8
    Public xform_rec29_w9
    Public xform_rec29_w10
    Public xform_rec29_w11
    Public xform_rec29_w12
    Public xform_rec29_w13
    Public xform_rec29_w14
    Public xform_rec29_w15
    Public xform_rec29_w16
    Public xform_rec29_w17
    Public xform_rec29_w18
    Public xform_rec29_w19
    Public xform_rec29_w20
    Public xform_rec29_w21
    Public xform_rec29_w22
    Public xform_rec29_w23
    Public xform_rec30_w0
    Public xform_rec30_w1
    Public xform_rec30_w2
    Public xform_rec30_w3
    Public xform_rec30_w4
    Public xform_rec30_w5
    Public xform_rec30_w6
    Public xform_rec30_w7
    Public xform_rec30_w8
    Public xform_rec30_w9
    Public xform_rec30_w10
    Public xform_rec30_w11
    Public xform_rec30_w12
    Public xform_rec30_w13
    Public xform_rec30_w14
    Public xform_rec30_w15
    Public xform_rec30_w16
    Public xform_rec30_w17
    Public xform_rec30_w18
    Public xform_rec30_w19
    Public xform_rec30_w20
    Public xform_rec30_w21
    Public xform_rec30_w22
    Public xform_rec30_w23
    Public xform_rec31_w0
    Public xform_rec31_w1
    Public xform_rec31_w2
    Public xform_rec31_w3
    Public xform_rec31_w4
    Public xform_rec31_w5
    Public xform_rec31_w6
    Public xform_rec31_w7
    Public xform_rec31_w8
    Public xform_rec31_w9
    Public xform_rec31_w10
    Public xform_rec31_w11
    Public xform_rec31_w12
    Public xform_rec31_w13
    Public xform_rec31_w14
    Public xform_rec31_w15
    Public xform_rec31_w16
    Public xform_rec31_w17
    Public xform_rec31_w18
    Public xform_rec31_w19
    Public xform_rec31_w20
    Public xform_rec31_w21
    Public xform_rec31_w22
    Public xform_rec31_w23


    Public default function Init(aBaseAddr)
        Set xform_rec0_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w0)(aBaseAddr, 32)
        Set xform_rec0_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w1)(aBaseAddr, 32)
        Set xform_rec0_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w2)(aBaseAddr, 32)
        Set xform_rec0_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w3)(aBaseAddr, 32)
        Set xform_rec0_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w4)(aBaseAddr, 32)
        Set xform_rec0_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w5)(aBaseAddr, 32)
        Set xform_rec0_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w6)(aBaseAddr, 32)
        Set xform_rec0_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w7)(aBaseAddr, 32)
        Set xform_rec0_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w8)(aBaseAddr, 32)
        Set xform_rec0_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w9)(aBaseAddr, 32)
        Set xform_rec0_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w10)(aBaseAddr, 32)
        Set xform_rec0_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w11)(aBaseAddr, 32)
        Set xform_rec0_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w12)(aBaseAddr, 32)
        Set xform_rec0_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w13)(aBaseAddr, 32)
        Set xform_rec0_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w14)(aBaseAddr, 32)
        Set xform_rec0_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w15)(aBaseAddr, 32)
        Set xform_rec0_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w16)(aBaseAddr, 32)
        Set xform_rec0_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w17)(aBaseAddr, 32)
        Set xform_rec0_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w18)(aBaseAddr, 32)
        Set xform_rec0_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w19)(aBaseAddr, 32)
        Set xform_rec0_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w20)(aBaseAddr, 32)
        Set xform_rec0_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w21)(aBaseAddr, 32)
        Set xform_rec0_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w22)(aBaseAddr, 32)
        Set xform_rec0_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec0_w23)(aBaseAddr, 32)
        Set xform_rec1_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w0)(aBaseAddr, 32)
        Set xform_rec1_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w1)(aBaseAddr, 32)
        Set xform_rec1_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w2)(aBaseAddr, 32)
        Set xform_rec1_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w3)(aBaseAddr, 32)
        Set xform_rec1_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w4)(aBaseAddr, 32)
        Set xform_rec1_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w5)(aBaseAddr, 32)
        Set xform_rec1_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w6)(aBaseAddr, 32)
        Set xform_rec1_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w7)(aBaseAddr, 32)
        Set xform_rec1_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w8)(aBaseAddr, 32)
        Set xform_rec1_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w9)(aBaseAddr, 32)
        Set xform_rec1_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w10)(aBaseAddr, 32)
        Set xform_rec1_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w11)(aBaseAddr, 32)
        Set xform_rec1_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w12)(aBaseAddr, 32)
        Set xform_rec1_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w13)(aBaseAddr, 32)
        Set xform_rec1_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w14)(aBaseAddr, 32)
        Set xform_rec1_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w15)(aBaseAddr, 32)
        Set xform_rec1_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w16)(aBaseAddr, 32)
        Set xform_rec1_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w17)(aBaseAddr, 32)
        Set xform_rec1_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w18)(aBaseAddr, 32)
        Set xform_rec1_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w19)(aBaseAddr, 32)
        Set xform_rec1_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w20)(aBaseAddr, 32)
        Set xform_rec1_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w21)(aBaseAddr, 32)
        Set xform_rec1_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w22)(aBaseAddr, 32)
        Set xform_rec1_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec1_w23)(aBaseAddr, 32)
        Set xform_rec2_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w0)(aBaseAddr, 32)
        Set xform_rec2_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w1)(aBaseAddr, 32)
        Set xform_rec2_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w2)(aBaseAddr, 32)
        Set xform_rec2_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w3)(aBaseAddr, 32)
        Set xform_rec2_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w4)(aBaseAddr, 32)
        Set xform_rec2_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w5)(aBaseAddr, 32)
        Set xform_rec2_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w6)(aBaseAddr, 32)
        Set xform_rec2_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w7)(aBaseAddr, 32)
        Set xform_rec2_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w8)(aBaseAddr, 32)
        Set xform_rec2_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w9)(aBaseAddr, 32)
        Set xform_rec2_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w10)(aBaseAddr, 32)
        Set xform_rec2_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w11)(aBaseAddr, 32)
        Set xform_rec2_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w12)(aBaseAddr, 32)
        Set xform_rec2_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w13)(aBaseAddr, 32)
        Set xform_rec2_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w14)(aBaseAddr, 32)
        Set xform_rec2_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w15)(aBaseAddr, 32)
        Set xform_rec2_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w16)(aBaseAddr, 32)
        Set xform_rec2_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w17)(aBaseAddr, 32)
        Set xform_rec2_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w18)(aBaseAddr, 32)
        Set xform_rec2_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w19)(aBaseAddr, 32)
        Set xform_rec2_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w20)(aBaseAddr, 32)
        Set xform_rec2_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w21)(aBaseAddr, 32)
        Set xform_rec2_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w22)(aBaseAddr, 32)
        Set xform_rec2_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec2_w23)(aBaseAddr, 32)
        Set xform_rec3_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w0)(aBaseAddr, 32)
        Set xform_rec3_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w1)(aBaseAddr, 32)
        Set xform_rec3_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w2)(aBaseAddr, 32)
        Set xform_rec3_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w3)(aBaseAddr, 32)
        Set xform_rec3_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w4)(aBaseAddr, 32)
        Set xform_rec3_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w5)(aBaseAddr, 32)
        Set xform_rec3_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w6)(aBaseAddr, 32)
        Set xform_rec3_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w7)(aBaseAddr, 32)
        Set xform_rec3_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w8)(aBaseAddr, 32)
        Set xform_rec3_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w9)(aBaseAddr, 32)
        Set xform_rec3_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w10)(aBaseAddr, 32)
        Set xform_rec3_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w11)(aBaseAddr, 32)
        Set xform_rec3_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w12)(aBaseAddr, 32)
        Set xform_rec3_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w13)(aBaseAddr, 32)
        Set xform_rec3_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w14)(aBaseAddr, 32)
        Set xform_rec3_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w15)(aBaseAddr, 32)
        Set xform_rec3_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w16)(aBaseAddr, 32)
        Set xform_rec3_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w17)(aBaseAddr, 32)
        Set xform_rec3_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w18)(aBaseAddr, 32)
        Set xform_rec3_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w19)(aBaseAddr, 32)
        Set xform_rec3_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w20)(aBaseAddr, 32)
        Set xform_rec3_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w21)(aBaseAddr, 32)
        Set xform_rec3_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w22)(aBaseAddr, 32)
        Set xform_rec3_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec3_w23)(aBaseAddr, 32)
        Set xform_rec4_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w0)(aBaseAddr, 32)
        Set xform_rec4_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w1)(aBaseAddr, 32)
        Set xform_rec4_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w2)(aBaseAddr, 32)
        Set xform_rec4_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w3)(aBaseAddr, 32)
        Set xform_rec4_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w4)(aBaseAddr, 32)
        Set xform_rec4_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w5)(aBaseAddr, 32)
        Set xform_rec4_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w6)(aBaseAddr, 32)
        Set xform_rec4_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w7)(aBaseAddr, 32)
        Set xform_rec4_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w8)(aBaseAddr, 32)
        Set xform_rec4_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w9)(aBaseAddr, 32)
        Set xform_rec4_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w10)(aBaseAddr, 32)
        Set xform_rec4_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w11)(aBaseAddr, 32)
        Set xform_rec4_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w12)(aBaseAddr, 32)
        Set xform_rec4_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w13)(aBaseAddr, 32)
        Set xform_rec4_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w14)(aBaseAddr, 32)
        Set xform_rec4_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w15)(aBaseAddr, 32)
        Set xform_rec4_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w16)(aBaseAddr, 32)
        Set xform_rec4_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w17)(aBaseAddr, 32)
        Set xform_rec4_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w18)(aBaseAddr, 32)
        Set xform_rec4_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w19)(aBaseAddr, 32)
        Set xform_rec4_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w20)(aBaseAddr, 32)
        Set xform_rec4_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w21)(aBaseAddr, 32)
        Set xform_rec4_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w22)(aBaseAddr, 32)
        Set xform_rec4_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec4_w23)(aBaseAddr, 32)
        Set xform_rec5_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w0)(aBaseAddr, 32)
        Set xform_rec5_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w1)(aBaseAddr, 32)
        Set xform_rec5_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w2)(aBaseAddr, 32)
        Set xform_rec5_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w3)(aBaseAddr, 32)
        Set xform_rec5_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w4)(aBaseAddr, 32)
        Set xform_rec5_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w5)(aBaseAddr, 32)
        Set xform_rec5_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w6)(aBaseAddr, 32)
        Set xform_rec5_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w7)(aBaseAddr, 32)
        Set xform_rec5_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w8)(aBaseAddr, 32)
        Set xform_rec5_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w9)(aBaseAddr, 32)
        Set xform_rec5_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w10)(aBaseAddr, 32)
        Set xform_rec5_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w11)(aBaseAddr, 32)
        Set xform_rec5_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w12)(aBaseAddr, 32)
        Set xform_rec5_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w13)(aBaseAddr, 32)
        Set xform_rec5_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w14)(aBaseAddr, 32)
        Set xform_rec5_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w15)(aBaseAddr, 32)
        Set xform_rec5_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w16)(aBaseAddr, 32)
        Set xform_rec5_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w17)(aBaseAddr, 32)
        Set xform_rec5_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w18)(aBaseAddr, 32)
        Set xform_rec5_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w19)(aBaseAddr, 32)
        Set xform_rec5_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w20)(aBaseAddr, 32)
        Set xform_rec5_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w21)(aBaseAddr, 32)
        Set xform_rec5_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w22)(aBaseAddr, 32)
        Set xform_rec5_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec5_w23)(aBaseAddr, 32)
        Set xform_rec6_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w0)(aBaseAddr, 32)
        Set xform_rec6_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w1)(aBaseAddr, 32)
        Set xform_rec6_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w2)(aBaseAddr, 32)
        Set xform_rec6_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w3)(aBaseAddr, 32)
        Set xform_rec6_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w4)(aBaseAddr, 32)
        Set xform_rec6_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w5)(aBaseAddr, 32)
        Set xform_rec6_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w6)(aBaseAddr, 32)
        Set xform_rec6_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w7)(aBaseAddr, 32)
        Set xform_rec6_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w8)(aBaseAddr, 32)
        Set xform_rec6_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w9)(aBaseAddr, 32)
        Set xform_rec6_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w10)(aBaseAddr, 32)
        Set xform_rec6_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w11)(aBaseAddr, 32)
        Set xform_rec6_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w12)(aBaseAddr, 32)
        Set xform_rec6_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w13)(aBaseAddr, 32)
        Set xform_rec6_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w14)(aBaseAddr, 32)
        Set xform_rec6_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w15)(aBaseAddr, 32)
        Set xform_rec6_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w16)(aBaseAddr, 32)
        Set xform_rec6_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w17)(aBaseAddr, 32)
        Set xform_rec6_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w18)(aBaseAddr, 32)
        Set xform_rec6_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w19)(aBaseAddr, 32)
        Set xform_rec6_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w20)(aBaseAddr, 32)
        Set xform_rec6_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w21)(aBaseAddr, 32)
        Set xform_rec6_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w22)(aBaseAddr, 32)
        Set xform_rec6_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec6_w23)(aBaseAddr, 32)
        Set xform_rec7_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w0)(aBaseAddr, 32)
        Set xform_rec7_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w1)(aBaseAddr, 32)
        Set xform_rec7_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w2)(aBaseAddr, 32)
        Set xform_rec7_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w3)(aBaseAddr, 32)
        Set xform_rec7_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w4)(aBaseAddr, 32)
        Set xform_rec7_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w5)(aBaseAddr, 32)
        Set xform_rec7_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w6)(aBaseAddr, 32)
        Set xform_rec7_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w7)(aBaseAddr, 32)
        Set xform_rec7_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w8)(aBaseAddr, 32)
        Set xform_rec7_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w9)(aBaseAddr, 32)
        Set xform_rec7_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w10)(aBaseAddr, 32)
        Set xform_rec7_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w11)(aBaseAddr, 32)
        Set xform_rec7_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w12)(aBaseAddr, 32)
        Set xform_rec7_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w13)(aBaseAddr, 32)
        Set xform_rec7_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w14)(aBaseAddr, 32)
        Set xform_rec7_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w15)(aBaseAddr, 32)
        Set xform_rec7_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w16)(aBaseAddr, 32)
        Set xform_rec7_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w17)(aBaseAddr, 32)
        Set xform_rec7_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w18)(aBaseAddr, 32)
        Set xform_rec7_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w19)(aBaseAddr, 32)
        Set xform_rec7_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w20)(aBaseAddr, 32)
        Set xform_rec7_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w21)(aBaseAddr, 32)
        Set xform_rec7_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w22)(aBaseAddr, 32)
        Set xform_rec7_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec7_w23)(aBaseAddr, 32)
        Set xform_rec8_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w0)(aBaseAddr, 32)
        Set xform_rec8_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w1)(aBaseAddr, 32)
        Set xform_rec8_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w2)(aBaseAddr, 32)
        Set xform_rec8_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w3)(aBaseAddr, 32)
        Set xform_rec8_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w4)(aBaseAddr, 32)
        Set xform_rec8_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w5)(aBaseAddr, 32)
        Set xform_rec8_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w6)(aBaseAddr, 32)
        Set xform_rec8_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w7)(aBaseAddr, 32)
        Set xform_rec8_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w8)(aBaseAddr, 32)
        Set xform_rec8_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w9)(aBaseAddr, 32)
        Set xform_rec8_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w10)(aBaseAddr, 32)
        Set xform_rec8_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w11)(aBaseAddr, 32)
        Set xform_rec8_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w12)(aBaseAddr, 32)
        Set xform_rec8_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w13)(aBaseAddr, 32)
        Set xform_rec8_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w14)(aBaseAddr, 32)
        Set xform_rec8_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w15)(aBaseAddr, 32)
        Set xform_rec8_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w16)(aBaseAddr, 32)
        Set xform_rec8_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w17)(aBaseAddr, 32)
        Set xform_rec8_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w18)(aBaseAddr, 32)
        Set xform_rec8_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w19)(aBaseAddr, 32)
        Set xform_rec8_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w20)(aBaseAddr, 32)
        Set xform_rec8_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w21)(aBaseAddr, 32)
        Set xform_rec8_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w22)(aBaseAddr, 32)
        Set xform_rec8_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec8_w23)(aBaseAddr, 32)
        Set xform_rec9_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w0)(aBaseAddr, 32)
        Set xform_rec9_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w1)(aBaseAddr, 32)
        Set xform_rec9_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w2)(aBaseAddr, 32)
        Set xform_rec9_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w3)(aBaseAddr, 32)
        Set xform_rec9_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w4)(aBaseAddr, 32)
        Set xform_rec9_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w5)(aBaseAddr, 32)
        Set xform_rec9_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w6)(aBaseAddr, 32)
        Set xform_rec9_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w7)(aBaseAddr, 32)
        Set xform_rec9_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w8)(aBaseAddr, 32)
        Set xform_rec9_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w9)(aBaseAddr, 32)
        Set xform_rec9_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w10)(aBaseAddr, 32)
        Set xform_rec9_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w11)(aBaseAddr, 32)
        Set xform_rec9_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w12)(aBaseAddr, 32)
        Set xform_rec9_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w13)(aBaseAddr, 32)
        Set xform_rec9_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w14)(aBaseAddr, 32)
        Set xform_rec9_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w15)(aBaseAddr, 32)
        Set xform_rec9_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w16)(aBaseAddr, 32)
        Set xform_rec9_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w17)(aBaseAddr, 32)
        Set xform_rec9_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w18)(aBaseAddr, 32)
        Set xform_rec9_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w19)(aBaseAddr, 32)
        Set xform_rec9_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w20)(aBaseAddr, 32)
        Set xform_rec9_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w21)(aBaseAddr, 32)
        Set xform_rec9_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w22)(aBaseAddr, 32)
        Set xform_rec9_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec9_w23)(aBaseAddr, 32)
        Set xform_rec10_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w0)(aBaseAddr, 32)
        Set xform_rec10_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w1)(aBaseAddr, 32)
        Set xform_rec10_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w2)(aBaseAddr, 32)
        Set xform_rec10_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w3)(aBaseAddr, 32)
        Set xform_rec10_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w4)(aBaseAddr, 32)
        Set xform_rec10_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w5)(aBaseAddr, 32)
        Set xform_rec10_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w6)(aBaseAddr, 32)
        Set xform_rec10_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w7)(aBaseAddr, 32)
        Set xform_rec10_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w8)(aBaseAddr, 32)
        Set xform_rec10_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w9)(aBaseAddr, 32)
        Set xform_rec10_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w10)(aBaseAddr, 32)
        Set xform_rec10_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w11)(aBaseAddr, 32)
        Set xform_rec10_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w12)(aBaseAddr, 32)
        Set xform_rec10_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w13)(aBaseAddr, 32)
        Set xform_rec10_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w14)(aBaseAddr, 32)
        Set xform_rec10_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w15)(aBaseAddr, 32)
        Set xform_rec10_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w16)(aBaseAddr, 32)
        Set xform_rec10_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w17)(aBaseAddr, 32)
        Set xform_rec10_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w18)(aBaseAddr, 32)
        Set xform_rec10_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w19)(aBaseAddr, 32)
        Set xform_rec10_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w20)(aBaseAddr, 32)
        Set xform_rec10_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w21)(aBaseAddr, 32)
        Set xform_rec10_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w22)(aBaseAddr, 32)
        Set xform_rec10_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec10_w23)(aBaseAddr, 32)
        Set xform_rec11_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w0)(aBaseAddr, 32)
        Set xform_rec11_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w1)(aBaseAddr, 32)
        Set xform_rec11_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w2)(aBaseAddr, 32)
        Set xform_rec11_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w3)(aBaseAddr, 32)
        Set xform_rec11_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w4)(aBaseAddr, 32)
        Set xform_rec11_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w5)(aBaseAddr, 32)
        Set xform_rec11_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w6)(aBaseAddr, 32)
        Set xform_rec11_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w7)(aBaseAddr, 32)
        Set xform_rec11_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w8)(aBaseAddr, 32)
        Set xform_rec11_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w9)(aBaseAddr, 32)
        Set xform_rec11_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w10)(aBaseAddr, 32)
        Set xform_rec11_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w11)(aBaseAddr, 32)
        Set xform_rec11_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w12)(aBaseAddr, 32)
        Set xform_rec11_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w13)(aBaseAddr, 32)
        Set xform_rec11_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w14)(aBaseAddr, 32)
        Set xform_rec11_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w15)(aBaseAddr, 32)
        Set xform_rec11_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w16)(aBaseAddr, 32)
        Set xform_rec11_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w17)(aBaseAddr, 32)
        Set xform_rec11_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w18)(aBaseAddr, 32)
        Set xform_rec11_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w19)(aBaseAddr, 32)
        Set xform_rec11_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w20)(aBaseAddr, 32)
        Set xform_rec11_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w21)(aBaseAddr, 32)
        Set xform_rec11_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w22)(aBaseAddr, 32)
        Set xform_rec11_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec11_w23)(aBaseAddr, 32)
        Set xform_rec12_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w0)(aBaseAddr, 32)
        Set xform_rec12_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w1)(aBaseAddr, 32)
        Set xform_rec12_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w2)(aBaseAddr, 32)
        Set xform_rec12_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w3)(aBaseAddr, 32)
        Set xform_rec12_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w4)(aBaseAddr, 32)
        Set xform_rec12_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w5)(aBaseAddr, 32)
        Set xform_rec12_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w6)(aBaseAddr, 32)
        Set xform_rec12_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w7)(aBaseAddr, 32)
        Set xform_rec12_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w8)(aBaseAddr, 32)
        Set xform_rec12_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w9)(aBaseAddr, 32)
        Set xform_rec12_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w10)(aBaseAddr, 32)
        Set xform_rec12_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w11)(aBaseAddr, 32)
        Set xform_rec12_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w12)(aBaseAddr, 32)
        Set xform_rec12_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w13)(aBaseAddr, 32)
        Set xform_rec12_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w14)(aBaseAddr, 32)
        Set xform_rec12_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w15)(aBaseAddr, 32)
        Set xform_rec12_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w16)(aBaseAddr, 32)
        Set xform_rec12_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w17)(aBaseAddr, 32)
        Set xform_rec12_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w18)(aBaseAddr, 32)
        Set xform_rec12_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w19)(aBaseAddr, 32)
        Set xform_rec12_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w20)(aBaseAddr, 32)
        Set xform_rec12_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w21)(aBaseAddr, 32)
        Set xform_rec12_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w22)(aBaseAddr, 32)
        Set xform_rec12_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec12_w23)(aBaseAddr, 32)
        Set xform_rec13_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w0)(aBaseAddr, 32)
        Set xform_rec13_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w1)(aBaseAddr, 32)
        Set xform_rec13_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w2)(aBaseAddr, 32)
        Set xform_rec13_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w3)(aBaseAddr, 32)
        Set xform_rec13_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w4)(aBaseAddr, 32)
        Set xform_rec13_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w5)(aBaseAddr, 32)
        Set xform_rec13_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w6)(aBaseAddr, 32)
        Set xform_rec13_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w7)(aBaseAddr, 32)
        Set xform_rec13_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w8)(aBaseAddr, 32)
        Set xform_rec13_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w9)(aBaseAddr, 32)
        Set xform_rec13_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w10)(aBaseAddr, 32)
        Set xform_rec13_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w11)(aBaseAddr, 32)
        Set xform_rec13_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w12)(aBaseAddr, 32)
        Set xform_rec13_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w13)(aBaseAddr, 32)
        Set xform_rec13_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w14)(aBaseAddr, 32)
        Set xform_rec13_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w15)(aBaseAddr, 32)
        Set xform_rec13_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w16)(aBaseAddr, 32)
        Set xform_rec13_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w17)(aBaseAddr, 32)
        Set xform_rec13_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w18)(aBaseAddr, 32)
        Set xform_rec13_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w19)(aBaseAddr, 32)
        Set xform_rec13_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w20)(aBaseAddr, 32)
        Set xform_rec13_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w21)(aBaseAddr, 32)
        Set xform_rec13_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w22)(aBaseAddr, 32)
        Set xform_rec13_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec13_w23)(aBaseAddr, 32)
        Set xform_rec14_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w0)(aBaseAddr, 32)
        Set xform_rec14_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w1)(aBaseAddr, 32)
        Set xform_rec14_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w2)(aBaseAddr, 32)
        Set xform_rec14_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w3)(aBaseAddr, 32)
        Set xform_rec14_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w4)(aBaseAddr, 32)
        Set xform_rec14_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w5)(aBaseAddr, 32)
        Set xform_rec14_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w6)(aBaseAddr, 32)
        Set xform_rec14_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w7)(aBaseAddr, 32)
        Set xform_rec14_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w8)(aBaseAddr, 32)
        Set xform_rec14_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w9)(aBaseAddr, 32)
        Set xform_rec14_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w10)(aBaseAddr, 32)
        Set xform_rec14_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w11)(aBaseAddr, 32)
        Set xform_rec14_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w12)(aBaseAddr, 32)
        Set xform_rec14_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w13)(aBaseAddr, 32)
        Set xform_rec14_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w14)(aBaseAddr, 32)
        Set xform_rec14_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w15)(aBaseAddr, 32)
        Set xform_rec14_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w16)(aBaseAddr, 32)
        Set xform_rec14_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w17)(aBaseAddr, 32)
        Set xform_rec14_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w18)(aBaseAddr, 32)
        Set xform_rec14_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w19)(aBaseAddr, 32)
        Set xform_rec14_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w20)(aBaseAddr, 32)
        Set xform_rec14_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w21)(aBaseAddr, 32)
        Set xform_rec14_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w22)(aBaseAddr, 32)
        Set xform_rec14_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec14_w23)(aBaseAddr, 32)
        Set xform_rec15_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w0)(aBaseAddr, 32)
        Set xform_rec15_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w1)(aBaseAddr, 32)
        Set xform_rec15_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w2)(aBaseAddr, 32)
        Set xform_rec15_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w3)(aBaseAddr, 32)
        Set xform_rec15_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w4)(aBaseAddr, 32)
        Set xform_rec15_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w5)(aBaseAddr, 32)
        Set xform_rec15_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w6)(aBaseAddr, 32)
        Set xform_rec15_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w7)(aBaseAddr, 32)
        Set xform_rec15_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w8)(aBaseAddr, 32)
        Set xform_rec15_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w9)(aBaseAddr, 32)
        Set xform_rec15_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w10)(aBaseAddr, 32)
        Set xform_rec15_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w11)(aBaseAddr, 32)
        Set xform_rec15_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w12)(aBaseAddr, 32)
        Set xform_rec15_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w13)(aBaseAddr, 32)
        Set xform_rec15_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w14)(aBaseAddr, 32)
        Set xform_rec15_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w15)(aBaseAddr, 32)
        Set xform_rec15_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w16)(aBaseAddr, 32)
        Set xform_rec15_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w17)(aBaseAddr, 32)
        Set xform_rec15_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w18)(aBaseAddr, 32)
        Set xform_rec15_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w19)(aBaseAddr, 32)
        Set xform_rec15_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w20)(aBaseAddr, 32)
        Set xform_rec15_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w21)(aBaseAddr, 32)
        Set xform_rec15_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w22)(aBaseAddr, 32)
        Set xform_rec15_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec15_w23)(aBaseAddr, 32)
        Set xform_rec16_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w0)(aBaseAddr, 32)
        Set xform_rec16_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w1)(aBaseAddr, 32)
        Set xform_rec16_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w2)(aBaseAddr, 32)
        Set xform_rec16_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w3)(aBaseAddr, 32)
        Set xform_rec16_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w4)(aBaseAddr, 32)
        Set xform_rec16_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w5)(aBaseAddr, 32)
        Set xform_rec16_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w6)(aBaseAddr, 32)
        Set xform_rec16_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w7)(aBaseAddr, 32)
        Set xform_rec16_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w8)(aBaseAddr, 32)
        Set xform_rec16_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w9)(aBaseAddr, 32)
        Set xform_rec16_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w10)(aBaseAddr, 32)
        Set xform_rec16_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w11)(aBaseAddr, 32)
        Set xform_rec16_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w12)(aBaseAddr, 32)
        Set xform_rec16_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w13)(aBaseAddr, 32)
        Set xform_rec16_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w14)(aBaseAddr, 32)
        Set xform_rec16_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w15)(aBaseAddr, 32)
        Set xform_rec16_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w16)(aBaseAddr, 32)
        Set xform_rec16_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w17)(aBaseAddr, 32)
        Set xform_rec16_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w18)(aBaseAddr, 32)
        Set xform_rec16_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w19)(aBaseAddr, 32)
        Set xform_rec16_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w20)(aBaseAddr, 32)
        Set xform_rec16_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w21)(aBaseAddr, 32)
        Set xform_rec16_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w22)(aBaseAddr, 32)
        Set xform_rec16_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec16_w23)(aBaseAddr, 32)
        Set xform_rec17_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w0)(aBaseAddr, 32)
        Set xform_rec17_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w1)(aBaseAddr, 32)
        Set xform_rec17_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w2)(aBaseAddr, 32)
        Set xform_rec17_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w3)(aBaseAddr, 32)
        Set xform_rec17_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w4)(aBaseAddr, 32)
        Set xform_rec17_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w5)(aBaseAddr, 32)
        Set xform_rec17_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w6)(aBaseAddr, 32)
        Set xform_rec17_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w7)(aBaseAddr, 32)
        Set xform_rec17_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w8)(aBaseAddr, 32)
        Set xform_rec17_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w9)(aBaseAddr, 32)
        Set xform_rec17_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w10)(aBaseAddr, 32)
        Set xform_rec17_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w11)(aBaseAddr, 32)
        Set xform_rec17_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w12)(aBaseAddr, 32)
        Set xform_rec17_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w13)(aBaseAddr, 32)
        Set xform_rec17_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w14)(aBaseAddr, 32)
        Set xform_rec17_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w15)(aBaseAddr, 32)
        Set xform_rec17_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w16)(aBaseAddr, 32)
        Set xform_rec17_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w17)(aBaseAddr, 32)
        Set xform_rec17_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w18)(aBaseAddr, 32)
        Set xform_rec17_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w19)(aBaseAddr, 32)
        Set xform_rec17_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w20)(aBaseAddr, 32)
        Set xform_rec17_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w21)(aBaseAddr, 32)
        Set xform_rec17_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w22)(aBaseAddr, 32)
        Set xform_rec17_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec17_w23)(aBaseAddr, 32)
        Set xform_rec18_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w0)(aBaseAddr, 32)
        Set xform_rec18_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w1)(aBaseAddr, 32)
        Set xform_rec18_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w2)(aBaseAddr, 32)
        Set xform_rec18_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w3)(aBaseAddr, 32)
        Set xform_rec18_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w4)(aBaseAddr, 32)
        Set xform_rec18_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w5)(aBaseAddr, 32)
        Set xform_rec18_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w6)(aBaseAddr, 32)
        Set xform_rec18_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w7)(aBaseAddr, 32)
        Set xform_rec18_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w8)(aBaseAddr, 32)
        Set xform_rec18_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w9)(aBaseAddr, 32)
        Set xform_rec18_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w10)(aBaseAddr, 32)
        Set xform_rec18_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w11)(aBaseAddr, 32)
        Set xform_rec18_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w12)(aBaseAddr, 32)
        Set xform_rec18_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w13)(aBaseAddr, 32)
        Set xform_rec18_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w14)(aBaseAddr, 32)
        Set xform_rec18_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w15)(aBaseAddr, 32)
        Set xform_rec18_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w16)(aBaseAddr, 32)
        Set xform_rec18_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w17)(aBaseAddr, 32)
        Set xform_rec18_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w18)(aBaseAddr, 32)
        Set xform_rec18_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w19)(aBaseAddr, 32)
        Set xform_rec18_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w20)(aBaseAddr, 32)
        Set xform_rec18_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w21)(aBaseAddr, 32)
        Set xform_rec18_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w22)(aBaseAddr, 32)
        Set xform_rec18_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec18_w23)(aBaseAddr, 32)
        Set xform_rec19_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w0)(aBaseAddr, 32)
        Set xform_rec19_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w1)(aBaseAddr, 32)
        Set xform_rec19_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w2)(aBaseAddr, 32)
        Set xform_rec19_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w3)(aBaseAddr, 32)
        Set xform_rec19_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w4)(aBaseAddr, 32)
        Set xform_rec19_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w5)(aBaseAddr, 32)
        Set xform_rec19_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w6)(aBaseAddr, 32)
        Set xform_rec19_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w7)(aBaseAddr, 32)
        Set xform_rec19_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w8)(aBaseAddr, 32)
        Set xform_rec19_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w9)(aBaseAddr, 32)
        Set xform_rec19_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w10)(aBaseAddr, 32)
        Set xform_rec19_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w11)(aBaseAddr, 32)
        Set xform_rec19_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w12)(aBaseAddr, 32)
        Set xform_rec19_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w13)(aBaseAddr, 32)
        Set xform_rec19_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w14)(aBaseAddr, 32)
        Set xform_rec19_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w15)(aBaseAddr, 32)
        Set xform_rec19_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w16)(aBaseAddr, 32)
        Set xform_rec19_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w17)(aBaseAddr, 32)
        Set xform_rec19_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w18)(aBaseAddr, 32)
        Set xform_rec19_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w19)(aBaseAddr, 32)
        Set xform_rec19_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w20)(aBaseAddr, 32)
        Set xform_rec19_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w21)(aBaseAddr, 32)
        Set xform_rec19_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w22)(aBaseAddr, 32)
        Set xform_rec19_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec19_w23)(aBaseAddr, 32)
        Set xform_rec20_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w0)(aBaseAddr, 32)
        Set xform_rec20_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w1)(aBaseAddr, 32)
        Set xform_rec20_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w2)(aBaseAddr, 32)
        Set xform_rec20_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w3)(aBaseAddr, 32)
        Set xform_rec20_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w4)(aBaseAddr, 32)
        Set xform_rec20_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w5)(aBaseAddr, 32)
        Set xform_rec20_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w6)(aBaseAddr, 32)
        Set xform_rec20_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w7)(aBaseAddr, 32)
        Set xform_rec20_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w8)(aBaseAddr, 32)
        Set xform_rec20_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w9)(aBaseAddr, 32)
        Set xform_rec20_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w10)(aBaseAddr, 32)
        Set xform_rec20_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w11)(aBaseAddr, 32)
        Set xform_rec20_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w12)(aBaseAddr, 32)
        Set xform_rec20_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w13)(aBaseAddr, 32)
        Set xform_rec20_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w14)(aBaseAddr, 32)
        Set xform_rec20_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w15)(aBaseAddr, 32)
        Set xform_rec20_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w16)(aBaseAddr, 32)
        Set xform_rec20_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w17)(aBaseAddr, 32)
        Set xform_rec20_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w18)(aBaseAddr, 32)
        Set xform_rec20_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w19)(aBaseAddr, 32)
        Set xform_rec20_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w20)(aBaseAddr, 32)
        Set xform_rec20_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w21)(aBaseAddr, 32)
        Set xform_rec20_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w22)(aBaseAddr, 32)
        Set xform_rec20_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec20_w23)(aBaseAddr, 32)
        Set xform_rec21_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w0)(aBaseAddr, 32)
        Set xform_rec21_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w1)(aBaseAddr, 32)
        Set xform_rec21_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w2)(aBaseAddr, 32)
        Set xform_rec21_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w3)(aBaseAddr, 32)
        Set xform_rec21_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w4)(aBaseAddr, 32)
        Set xform_rec21_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w5)(aBaseAddr, 32)
        Set xform_rec21_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w6)(aBaseAddr, 32)
        Set xform_rec21_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w7)(aBaseAddr, 32)
        Set xform_rec21_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w8)(aBaseAddr, 32)
        Set xform_rec21_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w9)(aBaseAddr, 32)
        Set xform_rec21_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w10)(aBaseAddr, 32)
        Set xform_rec21_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w11)(aBaseAddr, 32)
        Set xform_rec21_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w12)(aBaseAddr, 32)
        Set xform_rec21_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w13)(aBaseAddr, 32)
        Set xform_rec21_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w14)(aBaseAddr, 32)
        Set xform_rec21_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w15)(aBaseAddr, 32)
        Set xform_rec21_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w16)(aBaseAddr, 32)
        Set xform_rec21_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w17)(aBaseAddr, 32)
        Set xform_rec21_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w18)(aBaseAddr, 32)
        Set xform_rec21_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w19)(aBaseAddr, 32)
        Set xform_rec21_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w20)(aBaseAddr, 32)
        Set xform_rec21_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w21)(aBaseAddr, 32)
        Set xform_rec21_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w22)(aBaseAddr, 32)
        Set xform_rec21_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec21_w23)(aBaseAddr, 32)
        Set xform_rec22_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w0)(aBaseAddr, 32)
        Set xform_rec22_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w1)(aBaseAddr, 32)
        Set xform_rec22_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w2)(aBaseAddr, 32)
        Set xform_rec22_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w3)(aBaseAddr, 32)
        Set xform_rec22_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w4)(aBaseAddr, 32)
        Set xform_rec22_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w5)(aBaseAddr, 32)
        Set xform_rec22_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w6)(aBaseAddr, 32)
        Set xform_rec22_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w7)(aBaseAddr, 32)
        Set xform_rec22_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w8)(aBaseAddr, 32)
        Set xform_rec22_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w9)(aBaseAddr, 32)
        Set xform_rec22_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w10)(aBaseAddr, 32)
        Set xform_rec22_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w11)(aBaseAddr, 32)
        Set xform_rec22_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w12)(aBaseAddr, 32)
        Set xform_rec22_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w13)(aBaseAddr, 32)
        Set xform_rec22_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w14)(aBaseAddr, 32)
        Set xform_rec22_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w15)(aBaseAddr, 32)
        Set xform_rec22_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w16)(aBaseAddr, 32)
        Set xform_rec22_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w17)(aBaseAddr, 32)
        Set xform_rec22_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w18)(aBaseAddr, 32)
        Set xform_rec22_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w19)(aBaseAddr, 32)
        Set xform_rec22_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w20)(aBaseAddr, 32)
        Set xform_rec22_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w21)(aBaseAddr, 32)
        Set xform_rec22_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w22)(aBaseAddr, 32)
        Set xform_rec22_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec22_w23)(aBaseAddr, 32)
        Set xform_rec23_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w0)(aBaseAddr, 32)
        Set xform_rec23_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w1)(aBaseAddr, 32)
        Set xform_rec23_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w2)(aBaseAddr, 32)
        Set xform_rec23_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w3)(aBaseAddr, 32)
        Set xform_rec23_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w4)(aBaseAddr, 32)
        Set xform_rec23_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w5)(aBaseAddr, 32)
        Set xform_rec23_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w6)(aBaseAddr, 32)
        Set xform_rec23_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w7)(aBaseAddr, 32)
        Set xform_rec23_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w8)(aBaseAddr, 32)
        Set xform_rec23_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w9)(aBaseAddr, 32)
        Set xform_rec23_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w10)(aBaseAddr, 32)
        Set xform_rec23_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w11)(aBaseAddr, 32)
        Set xform_rec23_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w12)(aBaseAddr, 32)
        Set xform_rec23_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w13)(aBaseAddr, 32)
        Set xform_rec23_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w14)(aBaseAddr, 32)
        Set xform_rec23_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w15)(aBaseAddr, 32)
        Set xform_rec23_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w16)(aBaseAddr, 32)
        Set xform_rec23_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w17)(aBaseAddr, 32)
        Set xform_rec23_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w18)(aBaseAddr, 32)
        Set xform_rec23_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w19)(aBaseAddr, 32)
        Set xform_rec23_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w20)(aBaseAddr, 32)
        Set xform_rec23_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w21)(aBaseAddr, 32)
        Set xform_rec23_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w22)(aBaseAddr, 32)
        Set xform_rec23_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec23_w23)(aBaseAddr, 32)
        Set xform_rec24_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w0)(aBaseAddr, 32)
        Set xform_rec24_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w1)(aBaseAddr, 32)
        Set xform_rec24_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w2)(aBaseAddr, 32)
        Set xform_rec24_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w3)(aBaseAddr, 32)
        Set xform_rec24_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w4)(aBaseAddr, 32)
        Set xform_rec24_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w5)(aBaseAddr, 32)
        Set xform_rec24_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w6)(aBaseAddr, 32)
        Set xform_rec24_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w7)(aBaseAddr, 32)
        Set xform_rec24_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w8)(aBaseAddr, 32)
        Set xform_rec24_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w9)(aBaseAddr, 32)
        Set xform_rec24_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w10)(aBaseAddr, 32)
        Set xform_rec24_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w11)(aBaseAddr, 32)
        Set xform_rec24_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w12)(aBaseAddr, 32)
        Set xform_rec24_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w13)(aBaseAddr, 32)
        Set xform_rec24_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w14)(aBaseAddr, 32)
        Set xform_rec24_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w15)(aBaseAddr, 32)
        Set xform_rec24_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w16)(aBaseAddr, 32)
        Set xform_rec24_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w17)(aBaseAddr, 32)
        Set xform_rec24_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w18)(aBaseAddr, 32)
        Set xform_rec24_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w19)(aBaseAddr, 32)
        Set xform_rec24_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w20)(aBaseAddr, 32)
        Set xform_rec24_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w21)(aBaseAddr, 32)
        Set xform_rec24_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w22)(aBaseAddr, 32)
        Set xform_rec24_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec24_w23)(aBaseAddr, 32)
        Set xform_rec25_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w0)(aBaseAddr, 32)
        Set xform_rec25_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w1)(aBaseAddr, 32)
        Set xform_rec25_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w2)(aBaseAddr, 32)
        Set xform_rec25_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w3)(aBaseAddr, 32)
        Set xform_rec25_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w4)(aBaseAddr, 32)
        Set xform_rec25_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w5)(aBaseAddr, 32)
        Set xform_rec25_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w6)(aBaseAddr, 32)
        Set xform_rec25_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w7)(aBaseAddr, 32)
        Set xform_rec25_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w8)(aBaseAddr, 32)
        Set xform_rec25_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w9)(aBaseAddr, 32)
        Set xform_rec25_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w10)(aBaseAddr, 32)
        Set xform_rec25_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w11)(aBaseAddr, 32)
        Set xform_rec25_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w12)(aBaseAddr, 32)
        Set xform_rec25_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w13)(aBaseAddr, 32)
        Set xform_rec25_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w14)(aBaseAddr, 32)
        Set xform_rec25_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w15)(aBaseAddr, 32)
        Set xform_rec25_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w16)(aBaseAddr, 32)
        Set xform_rec25_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w17)(aBaseAddr, 32)
        Set xform_rec25_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w18)(aBaseAddr, 32)
        Set xform_rec25_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w19)(aBaseAddr, 32)
        Set xform_rec25_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w20)(aBaseAddr, 32)
        Set xform_rec25_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w21)(aBaseAddr, 32)
        Set xform_rec25_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w22)(aBaseAddr, 32)
        Set xform_rec25_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec25_w23)(aBaseAddr, 32)
        Set xform_rec26_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w0)(aBaseAddr, 32)
        Set xform_rec26_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w1)(aBaseAddr, 32)
        Set xform_rec26_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w2)(aBaseAddr, 32)
        Set xform_rec26_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w3)(aBaseAddr, 32)
        Set xform_rec26_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w4)(aBaseAddr, 32)
        Set xform_rec26_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w5)(aBaseAddr, 32)
        Set xform_rec26_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w6)(aBaseAddr, 32)
        Set xform_rec26_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w7)(aBaseAddr, 32)
        Set xform_rec26_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w8)(aBaseAddr, 32)
        Set xform_rec26_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w9)(aBaseAddr, 32)
        Set xform_rec26_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w10)(aBaseAddr, 32)
        Set xform_rec26_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w11)(aBaseAddr, 32)
        Set xform_rec26_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w12)(aBaseAddr, 32)
        Set xform_rec26_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w13)(aBaseAddr, 32)
        Set xform_rec26_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w14)(aBaseAddr, 32)
        Set xform_rec26_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w15)(aBaseAddr, 32)
        Set xform_rec26_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w16)(aBaseAddr, 32)
        Set xform_rec26_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w17)(aBaseAddr, 32)
        Set xform_rec26_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w18)(aBaseAddr, 32)
        Set xform_rec26_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w19)(aBaseAddr, 32)
        Set xform_rec26_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w20)(aBaseAddr, 32)
        Set xform_rec26_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w21)(aBaseAddr, 32)
        Set xform_rec26_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w22)(aBaseAddr, 32)
        Set xform_rec26_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec26_w23)(aBaseAddr, 32)
        Set xform_rec27_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w0)(aBaseAddr, 32)
        Set xform_rec27_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w1)(aBaseAddr, 32)
        Set xform_rec27_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w2)(aBaseAddr, 32)
        Set xform_rec27_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w3)(aBaseAddr, 32)
        Set xform_rec27_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w4)(aBaseAddr, 32)
        Set xform_rec27_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w5)(aBaseAddr, 32)
        Set xform_rec27_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w6)(aBaseAddr, 32)
        Set xform_rec27_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w7)(aBaseAddr, 32)
        Set xform_rec27_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w8)(aBaseAddr, 32)
        Set xform_rec27_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w9)(aBaseAddr, 32)
        Set xform_rec27_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w10)(aBaseAddr, 32)
        Set xform_rec27_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w11)(aBaseAddr, 32)
        Set xform_rec27_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w12)(aBaseAddr, 32)
        Set xform_rec27_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w13)(aBaseAddr, 32)
        Set xform_rec27_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w14)(aBaseAddr, 32)
        Set xform_rec27_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w15)(aBaseAddr, 32)
        Set xform_rec27_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w16)(aBaseAddr, 32)
        Set xform_rec27_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w17)(aBaseAddr, 32)
        Set xform_rec27_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w18)(aBaseAddr, 32)
        Set xform_rec27_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w19)(aBaseAddr, 32)
        Set xform_rec27_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w20)(aBaseAddr, 32)
        Set xform_rec27_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w21)(aBaseAddr, 32)
        Set xform_rec27_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w22)(aBaseAddr, 32)
        Set xform_rec27_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec27_w23)(aBaseAddr, 32)
        Set xform_rec28_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w0)(aBaseAddr, 32)
        Set xform_rec28_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w1)(aBaseAddr, 32)
        Set xform_rec28_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w2)(aBaseAddr, 32)
        Set xform_rec28_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w3)(aBaseAddr, 32)
        Set xform_rec28_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w4)(aBaseAddr, 32)
        Set xform_rec28_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w5)(aBaseAddr, 32)
        Set xform_rec28_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w6)(aBaseAddr, 32)
        Set xform_rec28_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w7)(aBaseAddr, 32)
        Set xform_rec28_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w8)(aBaseAddr, 32)
        Set xform_rec28_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w9)(aBaseAddr, 32)
        Set xform_rec28_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w10)(aBaseAddr, 32)
        Set xform_rec28_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w11)(aBaseAddr, 32)
        Set xform_rec28_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w12)(aBaseAddr, 32)
        Set xform_rec28_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w13)(aBaseAddr, 32)
        Set xform_rec28_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w14)(aBaseAddr, 32)
        Set xform_rec28_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w15)(aBaseAddr, 32)
        Set xform_rec28_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w16)(aBaseAddr, 32)
        Set xform_rec28_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w17)(aBaseAddr, 32)
        Set xform_rec28_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w18)(aBaseAddr, 32)
        Set xform_rec28_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w19)(aBaseAddr, 32)
        Set xform_rec28_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w20)(aBaseAddr, 32)
        Set xform_rec28_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w21)(aBaseAddr, 32)
        Set xform_rec28_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w22)(aBaseAddr, 32)
        Set xform_rec28_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec28_w23)(aBaseAddr, 32)
        Set xform_rec29_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w0)(aBaseAddr, 32)
        Set xform_rec29_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w1)(aBaseAddr, 32)
        Set xform_rec29_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w2)(aBaseAddr, 32)
        Set xform_rec29_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w3)(aBaseAddr, 32)
        Set xform_rec29_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w4)(aBaseAddr, 32)
        Set xform_rec29_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w5)(aBaseAddr, 32)
        Set xform_rec29_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w6)(aBaseAddr, 32)
        Set xform_rec29_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w7)(aBaseAddr, 32)
        Set xform_rec29_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w8)(aBaseAddr, 32)
        Set xform_rec29_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w9)(aBaseAddr, 32)
        Set xform_rec29_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w10)(aBaseAddr, 32)
        Set xform_rec29_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w11)(aBaseAddr, 32)
        Set xform_rec29_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w12)(aBaseAddr, 32)
        Set xform_rec29_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w13)(aBaseAddr, 32)
        Set xform_rec29_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w14)(aBaseAddr, 32)
        Set xform_rec29_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w15)(aBaseAddr, 32)
        Set xform_rec29_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w16)(aBaseAddr, 32)
        Set xform_rec29_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w17)(aBaseAddr, 32)
        Set xform_rec29_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w18)(aBaseAddr, 32)
        Set xform_rec29_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w19)(aBaseAddr, 32)
        Set xform_rec29_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w20)(aBaseAddr, 32)
        Set xform_rec29_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w21)(aBaseAddr, 32)
        Set xform_rec29_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w22)(aBaseAddr, 32)
        Set xform_rec29_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec29_w23)(aBaseAddr, 32)
        Set xform_rec30_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w0)(aBaseAddr, 32)
        Set xform_rec30_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w1)(aBaseAddr, 32)
        Set xform_rec30_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w2)(aBaseAddr, 32)
        Set xform_rec30_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w3)(aBaseAddr, 32)
        Set xform_rec30_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w4)(aBaseAddr, 32)
        Set xform_rec30_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w5)(aBaseAddr, 32)
        Set xform_rec30_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w6)(aBaseAddr, 32)
        Set xform_rec30_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w7)(aBaseAddr, 32)
        Set xform_rec30_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w8)(aBaseAddr, 32)
        Set xform_rec30_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w9)(aBaseAddr, 32)
        Set xform_rec30_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w10)(aBaseAddr, 32)
        Set xform_rec30_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w11)(aBaseAddr, 32)
        Set xform_rec30_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w12)(aBaseAddr, 32)
        Set xform_rec30_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w13)(aBaseAddr, 32)
        Set xform_rec30_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w14)(aBaseAddr, 32)
        Set xform_rec30_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w15)(aBaseAddr, 32)
        Set xform_rec30_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w16)(aBaseAddr, 32)
        Set xform_rec30_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w17)(aBaseAddr, 32)
        Set xform_rec30_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w18)(aBaseAddr, 32)
        Set xform_rec30_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w19)(aBaseAddr, 32)
        Set xform_rec30_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w20)(aBaseAddr, 32)
        Set xform_rec30_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w21)(aBaseAddr, 32)
        Set xform_rec30_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w22)(aBaseAddr, 32)
        Set xform_rec30_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec30_w23)(aBaseAddr, 32)
        Set xform_rec31_w0 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w0)(aBaseAddr, 32)
        Set xform_rec31_w1 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w1)(aBaseAddr, 32)
        Set xform_rec31_w2 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w2)(aBaseAddr, 32)
        Set xform_rec31_w3 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w3)(aBaseAddr, 32)
        Set xform_rec31_w4 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w4)(aBaseAddr, 32)
        Set xform_rec31_w5 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w5)(aBaseAddr, 32)
        Set xform_rec31_w6 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w6)(aBaseAddr, 32)
        Set xform_rec31_w7 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w7)(aBaseAddr, 32)
        Set xform_rec31_w8 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w8)(aBaseAddr, 32)
        Set xform_rec31_w9 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w9)(aBaseAddr, 32)
        Set xform_rec31_w10 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w10)(aBaseAddr, 32)
        Set xform_rec31_w11 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w11)(aBaseAddr, 32)
        Set xform_rec31_w12 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w12)(aBaseAddr, 32)
        Set xform_rec31_w13 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w13)(aBaseAddr, 32)
        Set xform_rec31_w14 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w14)(aBaseAddr, 32)
        Set xform_rec31_w15 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w15)(aBaseAddr, 32)
        Set xform_rec31_w16 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w16)(aBaseAddr, 32)
        Set xform_rec31_w17 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w17)(aBaseAddr, 32)
        Set xform_rec31_w18 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w18)(aBaseAddr, 32)
        Set xform_rec31_w19 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w19)(aBaseAddr, 32)
        Set xform_rec31_w20 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w20)(aBaseAddr, 32)
        Set xform_rec31_w21 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w21)(aBaseAddr, 32)
        Set xform_rec31_w22 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w22)(aBaseAddr, 32)
        Set xform_rec31_w23 = (New REGISTER_EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_xform_rec31_w23)(aBaseAddr, 32)

        Set Init = Me
    end function

End Class


'***********************************

' Regsets

'***********************************


Set EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS = CreateObject("System.Collections.ArrayList")
EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS.Add ((New EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_INSTANCE)(&H4d010000))
EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS.Add ((New EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_INSTANCE)(&H4d110000))
EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS.Add ((New EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_INSTANCE)(&H4d210000))
EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS.Add ((New EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_INSTANCE)(&H4d310000))
EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS.Add ((New EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_INSTANCE)(&H4d410000))
EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS.Add ((New EIP165S_EIP_160S_E_32_BRCM_TRANSFORM_RECORDS_INSTANCE)(&H4d510000))


